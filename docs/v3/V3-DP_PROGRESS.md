# v3-DP PROGRESS LOG

*Maintained per `docs/v3/MASTER_BLUEPRINT_v3-DP.md`. One row per item as it moves; newest first. Statuses distinguish **not-started** from **merged-but-not-yet-live-proven** from **live-verified**, and carry the merge `<sha>` once landed (the same convention as `V3_PROGRESS.md`). The forward data plane is a **collection-first** workstream: several items only become *live-proven* after real data accrues (a recorded IPO for DP-1/DP-3; ~6–12 months for DP-4), so "merged + gated" and "live-proven" are deliberately separate columns here.*

> **v3-DP status:** workstream **OPEN**. **DP-1 (the recorder) is MERGED, DEPLOYED and COLLECTING** as of 2026-07-19 — the collection clock is running. It banked its first genuine sample on its first post-deploy cycle (13:54 UTC), because `cmll` happened to be mid-book; the predicted `idle — no IPO in window` state was never reached. **Its full live-proof is still pending and cannot be forced:** that means a COMPLETE trajectory across an IPO's open→close window (`cmll` closes 2026-07-21), not merely a first row. Until then the thing to watch is that the `Recorder` row stays green cycle after cycle — green-and-banking while a book is open, green-and-idle between IPOs, red if the cycle stops or a write fails. DP-2→DP-3 follow as a normal build sequence; DP-4 is a patient analysis item gated on accumulated data. The B1 wall (trajectory data reaches display + analysis, never the model) is enforced at every layer by the import-boundary test — **now armed and proven non-vacuous** against `ipo.series`.

| Date | Item | Type | Status | Notes |
|---|---|---|---|---|
| 2026-07-19 | **DP-1 — forward subscription recorder** | infra (collection) | **merged `eab1f85`** (+ `a725798`, `f94e9a6`, `1ff21db`); **DEPLOYED + LIVE + BANKING REAL DATA** (2026-07-19 13:36 UTC). First cycle 13:54 UTC banked its first genuine sample. **Full live-proof still pending:** a COMPLETE trajectory through an IPO's close (`cmll` closes 2026-07-21) | Piggybacks the `ipo-ingest.service` 30-min fetch — **one fetch, two writes**, no new timer, zero added NSE load. Three plan-first catches, each a defect avoided: **(1)** the blueprint's hook point (after the current-state upsert) **cannot see its data** — `NseSubscription`/`updateTime`/raw response have left scope by then, leaving only the lossy `list[IPORecord]`; capture moved to the fork's **success branch** (`live.py:122`), the *write* still lands after the upsert. **(2)** the **fabricated-row hazard** — `_degrade_subscription` synthesises a replay of the prior record stamped with the prior `captured_at`, so a post-upsert hook would bank rows indistinguishable from genuine ones; capture living only in the `try` makes a degraded reading *structurally* unable to reach the series (a failed fetch banks nothing — an honest gap). **(3)** the sample model had to live **outside `core/`** (inside the scoring guard), hence the new `ipo.series` package. **VM-only by construction:** the sink is created in `run_live_ingest.py`, NOT in `refresh_from_nse` — also the desktop's local-fallback leaf, which would have turned every shipped `.exe` into a recorder during a VM outage. **Store:** one file per IPO, `tmp`+`os.replace`; the load-bearing insight is that a series is **bounded** (~3 days ≈ ~150 samples) so it completes rather than grows — blast radius one IPO, no torn state observable at the live path (~26 MB/yr). Rejected: single-parquet rewrite (v2-A1's — a crash truncates the whole bank, the review's #2/#3), JSONL (a ~4.4 KB sample exceeds `PIPE_BUF`), SQLite. **Schema:** superset of the retired 11 cols incl. `source_update_time`; category set **varies per IPO** (25 vs 22 in fixtures) so `categories` is a variable-length typed list, `noOfShareOffered`/`noOfSharesBid` retained for DP-4, `schema_version` from row one. **No dedupe** (a flat stretch is signal). **Health = option A:** one `Recorder` row fans out to digest + `/status` + DEGRADED + the 20-min alert-check's existing repeat-suppression/recovery — **no second writer** to `alert_state.json`; separates *correctly idle* ("no IPO in window") from *broken*. systemd `OnFailure` **deliberately not overloaded** (its fixed message would contradict the digest). Two self-caught bugs: a **dark-ship violation** (STALE-for-never-run would have painted every VM DEGRADED on merge) and an **unreachable never-shrink guard** (fixed by extracting the `_merge` seam so it defends something real). **Proven:** `MAX\|Δprob\|=0.0` — 386 IPOs, 0 verdict/reason changes, **byte-identical dump** vs `main` (the definitive check, not the guard's proxy); **B1 wall non-vacuous** — importing `ipo.series.store` into `features/build.py` turns the boundary test RED naming `LEAK ipo.series,ipo.series.models,ipo.series.store`, green on revert; torn-write/corrupt-refuses/never-shrink/blast-radius durability; a raising sink still yields records. +24 tests, **541/0**. **Docs:** three on-box realities that were in no repo file — the real unit name `ipo-ingest.timer`/`.service`, the already-applied `context.env` drop-in the runbook still listed as pending, and the undocumented `OnFailure` drop-in — captured as reference copies under `operations/systemd/` (vm-user redacted per the public-repo policy, caught by the pre-merge IP-guard), plus a standing rule written down: **health derives from `ingest_state.json` + live probes, never from systemd unit state.** Console routing lands events in the `engine.log` family (durable/rotated/greppable) but, stated plainly, **not** desktop-console-visible — the recorder runs on the VM and the console reads the local log; a VM `/logs` surface is DP-2/DP-3-shaped follow-on, deliberately not built here. **DEPLOY (2026-07-19):** ten files + the new `src/ipo/series/` package scp'd to `/opt/ipo`; `ipo-telegram-bot` restarted (the one long-running unit — it holds `vm_status.build_status` in memory, so without it `/status` would have silently served a row set missing `Recorder` while the digest showed it). B1 wall re-verified **on the deployed box**, not just in CI. **First cycle exceeded the predicted `idle` state — it recorded immediately**, because `cmll` (open 07-17 → close 07-21) was live: `samples_last_cycle=1`, `in_window_last_cycle=1`, `last_error=null`, `cmll.json` written with 18 category rows, the raw share counts, the complete response, and a sha256. **The window gate proved itself on real data** — of three mainboard IPOs in the store, only `cmll` was banked; `alpinetex`/`sbifunds` (both closed 07-16) were correctly skipped. **Two design decisions validated on day one:** NSE's `source_update_time` came back `"Updated as on 17-Jul-2026 17:00:00"` against a `captured_at` of 07-19 — a 2-day-old book, because 07-19 is a **Sunday** and no bidding occurs at the weekend; without that stamp the row would have looked freshly-true. And the deliberate **no-dedupe** rule means the weekend's identical readings bank as an honest flat stretch ("the book did not move") rather than vanishing — exactly the "flat is signal" evidence DP-4 needs. **One post-deploy bug found by looking at the box, not by tests** (`f94e9a6`): the health read constructed a writer whose `__init__` mkdir'd, so a bot restart conjured an empty `data/series/` before the recorder had run — collapsing "deployed but never run" and "not deployed at all" into an identical on-disk signal. Fixed to create nothing, proven live on the box against a fresh dir, and the invariant is now pinned by test rather than by the docstring that had asserted it while the code did the opposite (`1ff21db`). |
| 2026-07-19 | **DP-2 — `/subscription-series` read route** | infra (VM) | **merged `5a4bfa7`** · **DEPLOYED + LIVE-PROVEN 2026-07-19** — served `cmll`'s real 3-sample trajectory on its first request after the `ipo-vm-api` restart | **AS BUILT.** Fourth GET route, one-IPO-scoped (`?ipo_id=` REQUIRED, not an optional filter), `{refreshed_at, ipo_id, samples}` envelope, per-IPO freshness. Inherits read-only / rate-limit / CORS / no-model from the existing API — a new `@app.get` is covered by `test_api_is_structurally_read_only` automatically. **THE VOLUME TRIM IS THE ONE REAL DIVERGENCE FROM THE PRE-BUILD PLAN,** and it came from measuring rather than assuming: scoping to one IPO does **not** contain volume on its own, because the *per-IPO* payload was itself the problem. Against the first genuinely banked sample a **stored row is 6,163 bytes** (DP-1 keeps the complete NSE response + ~18 category rows, since a field discarded at collection is unrecoverable), so a ~150-sample trajectory would be **~903 KB per detail-page open** on a 1 vCPU / 1 GB box. The **wire sample** therefore carries `captured_at` + `source_update_time` + the six multiples + `schema_version` — **376 bytes, ~55 KB per trajectory**; `raw_response` and `categories` **stay on disk for DP-4**, which reads the store directly on the VM rather than over HTTP. Nothing is lost, only un-shipped, and a test pins it so a later edit cannot quietly re-add ~900 KB. **Rate limit unchanged, arithmetic stated:** breaching the shared 60/min from one IP needs 60 detail-page opens in a minute, sustained one per second — no per-route budget, and no raising the global limit (which protects `/records`). **Per-IPO freshness** = the newest reading banked for *that* `ipo_id`, derived from the data rather than file mtime (a restore or `cp` rewrites mtime; `captured_at` survives being moved) — an open IPO is still growing while one closed last week is complete, so DP-3 must read this, not the app-global clock. **Honest empty envelope** for unknown/absent `ipo_id`, an unreadable file, or an id unsafe as a filename — never 404/500; a **missing `ipo_id` param** is the one genuine client error (clean 4xx) and is pinned *against its look-alike*, because an unknown IPO answering 4xx would say "you asked wrong" when the truth is "we never watched that one". **Corrupt-file case gets the WARNING, not just the 200** — the load-bearing half, since on the wire a corrupt series and a never-recorded one are identical and without the log a VM fault is indistinguishable from honest absence: tested against a committed truncated fixture, asserting 200 + empty + a `series_read_failed` warning that **names the `ipo_id`**, plus a **blast-radius companion** proving one corrupt file cannot poison another IPO's series. Delegates to `SubscriptionSeriesStore.read` (the reader whose id check refuses `../../etc/passwd`), never a fresh `json.load`. **Engine-side fetch deliberately NOT built:** a draft `VmClient.fetch_subscription_series` was **reverted** — no caller, no test, and `client.py` runs in the shipped `.exe` and never on the VM, so scp'ing it would ship a file to a box that never executes it; the engine-mediated fetch is **DP-3's decision, not yet locked**. The shared `SeriesEnvelope` in `vm/models.py` stays — the server validates against it today, DP-3's client end reuses it. Fixtures are committed JSON loaded by `Path` (the pre-existing `tests/fixtures` convention; the `series/` subdir is new, the pattern is not) in four shapes — full trajectory w/ weekend flat stretch, sparse, fetch-gap (real absence → DP-3 draws a broken line, never interpolated), never-recorded. Noted honestly: no fixture here is read by the frontend today, so python+node reuse is an **intention DP-3 must earn**, not an existing precedent. **Proven:** `MAX\|Δprob\|=0.0` (zero files under the scoring closure; guard clean) · **B1 wall green WITH the new series read in `vm/server.py` and still non-vacuous** (breach → RED naming the leak → revert → green; a closure walk shows `ipo.vm` and `ipo.series` both unreachable from the scoring path) · GET-only · bounded (`Retry-After` + CORS when limited) · honest empty · **zero new mypy errors**. +13 tests. Rebuilt on post-reword `main` so the pre-reword commits — whose messages carried the scrubbed vm-user literal — are not resurrected. **DEPLOYED 2026-07-19 (proof, not assertion).** Unit named at last: **`ipo-vm-api.service`** (`Type=simple, Restart=always`) — it is not a timer, so it never appeared in `list-timers`; reference copy now committed under `operations/systemd/` (vm-user scrubbed). scp'd `vm/server.py` + `vm/models.py` only (**not** `client.py` — engine-side), both verified byte-identical on the box by `git hash-object` (`3ed648b9` / `e13aef2f`). **Before/after, not asserted:** beforehand the running server advertised `/context /health /records` and answered **404** for `/subscription-series?ipo_id=cmll`; after the restart it advertises all four and answers **200** with the real trajectory. **Live-proven:** 3 samples oldest-first, `refreshed_at == max(captured_at)` (per-IPO freshness holding on real data), heavy fields **absent** from the wire, and the volume trim measured end-to-end — **18,495 B on disk → 1,061 B on the wire (~17×)**, matching the 6,163→376 per-sample projection. **The flat-stretch/no-dedupe decision confirmed on live data:** all three samples carry identical multiples *and* an unchanged NSE stamp of `17-Jul-2026 17:00:00` against captures at 19:24 / 19:54 / 20:24 on the 19th — a **Sunday**, no bidding — so the store honestly records "the book did not move" where the retired v2-A1 dedupe would have left a hole. **Degradation classes verified live:** real closed pre-recorder IPOs (`alpinetex`, `sbifunds`, both closed 07-16) → `200 {refreshed_at: null, samples: []}` (DP-3's whole-series-absent state against GENUINE absence, not a fixture); missing param → **422**; nonsense id and `../../etc/passwd` → 200 empty with no file read; `POST` → **405**; `/records` + `/context` unaffected by the restart. **Store still FULL on disk** — `raw_response` (4 NSE keys), `categories` (18 rows) and the sha256 present on all 3 samples, raw counts still reproducing the multiple exactly (`656740 / 2238805 = 0.29334`), so DP-4's read path is intact. Recorder untouched across the restart (separate oneshot unit). Also closed a 4th box-vs-repo drift found en route: the box's `vm/models.py` predated the BUG-4 docstring fix (`7b4de4f`) and had never been scp'd — docstring-only, zero code difference, verified by diff before overwriting. |
| 2026-07-19 | **DP-3a — engine-side series fetch (data path)** | infra (engine) | **merged `e3049f5`** — built + gated + reviewed. **NOT INDEPENDENTLY LIVE-PROVABLE:** it ships in the `.exe` together with DP-3b, so its live proof arrives with the chart, not before | **AS BUILT.** DP-3 was planned as one item and built as two — the engine data path (Python) and the chart (React) have different proof obligations and different ship surfaces. This is the engine half: `VmClient.fetch_series` (re-added, now with a caller, logging and tests — it was reverted at DP-2's merge precisely because it was dead code then) + a local `GET /subscription-series/{ipo_id}` that re-serves DP-2's route. **FOUR HONEST STATES, kept structurally distinct** — `recorded` (available=true, samples) · `not_recorded` (available=true, nothing ever banked) · `unavailable` (available=false, VM unreachable) · `not_loaded` (available=false, dark-ship). The load-bearing pair is `not_recorded` vs `unavailable`: "this IPO was never recorded" is a fact about the world, "we could not reach the VM" is a fact about our reachability, and they look identical on a naive wire (both no-samples). They differ in **both** `available` and `state`, and a test asserts all four signatures are mutually unique. **No local fallback** — the recorder is VM-only, so reporting `[]` on a VM outage would state an absence we cannot vouch for. **On-demand, NOT cached, NOT in the 30-min cycle:** the series is per-IPO and many rows and most IPOs are never opened, so pre-fetching every trajectory each cycle would recreate the volume problem DP-2 solved. **One home for the VM client:** it was built inside `_live_refresh`'s closure, invisible to `create_app`; hoisted to `build_vm_client()` and passed to both, with a test proving a page-open triggers **no** records/context fetch (`series_calls==1`, `records_calls==0`, `context_calls==0`). Approved side effect: `live_ingest` off + `VM_BASE_URL` set still serves the route — correct, it is on-demand. **The on-demand budget is pinned to MEASURED latency, not feel:** median 45 ms / min 38 ms / max 1.18 s over 10 runs on the real desktop→VM path (the ~1.2 s outliers are the 1-vCPU box under keepalive contention), giving `ON_DEMAND_TIMEOUT_SEC=5.0` (~4.2× the worst healthy response, so a contended-but-live box is not misreported down) and `ON_DEMAND_RETRIES=1` (worst case ~10 s vs the cycle's ~30 s) — named constants with inline rationale, not bare literals. Logs `series_from_vm` / `vm_series_unavailable` (warning, naming the `ipo_id`) into `engine.log` for the V3-16 console. **B1 WALL — the load-bearing proof here**, because DP-3a is the FIRST time the series types share the scorer's own process, so "the model cannot see it" stopped being true by geography: `ipo.vm` added to the boundary guard's forbidden prefixes (already unreachable — strengthens rather than fixes) and proven **non-vacuous three ways** — baseline green, then `import ipo.series.store` → RED `LEAK ipo.series,…`, `from ipo.vm.models import SeriesEnvelope` → RED `LEAK ipo.vm,ipo.vm.models`, `from ipo.vm.client import VmClient` → RED `LEAK ipo.vm,ipo.vm.client,ipo.vm.models`, green on revert. Also fixed the assertion message, which named only service/archive/series and would have caught an `ipo.vm` breach while mislabelling the leak (the same under-reporting fixed at DP-2). **`MAX\|Δprob\|=0.0` git-proven and load-bearing here** (engine-side Python, unlike DP-2's VM-only): zero files under `model/`\|`features/`\|`calibration/`\|`core/`\|`models/`\|`config/`; guard clean. +12 tests, 568/0, zero new mypy errors. **PWA untouched.** **DP-3b HANDOFF CONSTRAINTS:** (1) read `state`, **never** `samples.length` — length alone collapses `not_recorded` into `unavailable`; (2) show a **loading state** during the VM-down window, which can reach **~10 s** before the route answers `unavailable`, or the chart will flash an empty frame that reads as "never recorded". |
| 2026-07-19 | **DP-3b — subscription history chart (V3-9)** | feature (display) | **merged `a33f49a`** — built + gated + reviewed. **CODE-PROVEN, NOT YET LIVE-PROVEN:** it ships in the `.exe`, so the live proof is the visual pass on real data (cmll's curve, a History-5 empty frame, VM-down loading→unavailable) — a separate post-merge step | **AS BUILT.** The deferred V3-9, finally unblocked. One card after "Subscription (final)" on the detail page — not a tab, not a screen: the final-figures card answers *where did it end*, this answers *how did it get there*. Hand-rolled SVG in an `ss-*` namespace mirroring the app's existing `rd-*` reliability diagram — no charting library, no new visual language (V3-15); QIB takes the same `--apply` green the Subscription card already gives it, ticks are Fira Code 8px exactly like `.rd-tick`. **RENDERS OFF `state`, NEVER `samples.length`** — an empty list cannot distinguish "nothing was ever recorded" from "we could not reach the server", so sample count is deliberately not a branch condition anywhere. **Four states, visually distinct, all keeping the frame** (card + axes + chrome), so an empty field reads as *nothing to show* rather than *this IPO has no such thing*: `recorded` the curve · `not_recorded` "No subscription history recorded / This IPO closed before we started recording." · `unavailable` **amber**, "Couldn't reach the server — try again shortly." · `loading` shimmer + "Loading subscription history…" so the frame never flashes the empty state then changes its mind. **Within `recorded`:** a fetch gap **splits the polyline** (one per contiguous run) so a break is *absence of geometry*, never an interpolated bridge — the chart must not invent readings DP-1 refused to fabricate; a flat book is one run of unchanging values (recorded, unmoving, signal); a single sample renders as a **point** rather than vanishing. The gap threshold is **relative** (1.8× the median interval), not a hardcoded 30 min, so it survives a cadence retune. **Per-IPO freshness** from DP-3a's `refreshed_at` ("updating" open / "complete" closed), never the app-global clock; reading count is grammatical (1 reading / 2 readings). **Shows the BOOK, never a score** — raw multiples only; no probability-over-time, no verdict-over-time (parked #1 / re-scoring hazard). **PREVIEW-FIRST EARNED ITS COST — two findings a paper plan would have missed:** (1) on *real* `cmll` data NII and Retail render **~1px apart** (1.5927 vs 1.5776), so the chart looked like it had two lines — fixed by putting each series' **latest value in the legend**, so coincident lines cannot hide a series; (2) an infinitely-repeating SVG `<animate>` never lets the renderer settle and **wedges screenshot tooling** — shipped as a **CSS class animation** instead, which also honours `prefers-reduced-motion`. **Wire values vs on-screen words are separate concerns:** the `SeriesState` values are the DP-3a contract and appear in log lines (`state=unavailable` is the precision an operator wants) — unchanged; `MESSAGES` is the ONE place they become sentences a retail reader sees, with a test asserting **no internal vocabulary reaches the screen** ("data plane", "trajectory", "in this build"). **PROPERTY-GUARD REWRITE (the review's standout):** removing the "this doesn't mean nothing was recorded" disclaimer — correctly, it explained our own bookkeeping to a reader who never had the wrong idea — forced the test that pinned that *sentence* to change. Rather than delete the check (which is how guards quietly become decorative), it was replaced with a **property** check: the two states must name different subjects, only `unavailable` may wear amber, and — the critical negative — **the `unavailable` copy may never ASSERT an absence it cannot vouch for**. Proven non-vacuous: rewriting it to "No subscription history recorded" turns the suite RED, restoring goes green. The copy can now be reworded freely but can never claim empty when it merely failed to reach the server. **Proven:** `MAX\|Δprob\|=0.0` — machine-checkable, **zero Python files in the diff**, so no scoring-path edge is even *expressible*; guard clean · B1 wall green (`ipo.series`/`ipo.vm` still unreachable from the scoring closure) · **+16 frontend tests, 48 total** (`node --test`) · `tsc` clean · `vite build` green · the built bundle verified to carry the user-facing copy and none of the internal terms. The review mockup (`design/mockups/dp3b-series-chart.html`) is kept **in sync with the shipped `MESSAGES` and says so**, so the design artifact cannot quietly drift from what ships. **LOGGED, NOT DP-3b's TO FIX:** `"in this build"` is **pre-existing** app copy in the sync-status chip ("No live NSE feed configured in this build") — dev-speak on a user-facing surface, flagged for a future app-wide copy pass. |
| — | **DP-4 — close-day trajectory study** | analysis (read-only) | **NOT STARTED** — blocked on accumulated data (~6–12 mo) | Offline study: QIB fill curve by hour + noon-vs-final verdict-divergence counts (stayed-APPLY / crossed-into-APPLY / crossed-down) + the actionable-window verdict. Uses the production scorer/calibrator as-of each reading's own timestamp (no leakage). Produces **findings** that settle the parked #1 — changes no app code. **Measures, does not promote:** a positive result is a *B1 re-gate trigger*, a separate v2-protocol decision, NOT performed here. |

---

## CLOSED / PARKED (carried from the blueprint ledger)

- **B1 (trajectory-as-a-score-feature)** — graveyard; re-gate is a separate deliberate v2 decision, not reopened by v3-DP.
- **Code-review #1 (close-day emission)** — parked, awaits DP-4's measurement; any fix is display-layer, never re-scoring on partial books.
- **DP-1 store physical shape** — open by design, resolved in DP-1's build plan.
