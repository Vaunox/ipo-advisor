# v3-DP PROGRESS LOG

*Maintained per `docs/v3/MASTER_BLUEPRINT_v3-DP.md`. One row per item as it moves; newest first. Statuses distinguish **not-started** from **merged-but-not-yet-live-proven** from **live-verified**, and carry the merge `<sha>` once landed (the same convention as `V3_PROGRESS.md`). The forward data plane is a **collection-first** workstream: several items only become *live-proven* after real data accrues (a recorded IPO for DP-1/DP-3; ~6–12 months for DP-4), so "merged + gated" and "live-proven" are deliberately separate columns here.*

> **v3-DP status:** workstream **OPEN**. **DP-1 (the recorder) is MERGED (`eab1f85`, 2026-07-19)** — the anchor and the only time-sensitive item, so the collection clock starts the moment it is deployed to the VM. It **ships dark**: inert until the scp deploy, and then correctly idle (`Recorder  idle — no IPO in window`) until a real mainboard IPO opens its book. Its **live-proof is therefore genuinely pending** and cannot be forced — it waits for the first fully-recorded IPO, which is expected, not a gap. DP-2→DP-3 follow as a normal build sequence; DP-4 is a patient analysis item gated on accumulated data. The B1 wall (trajectory data reaches display + analysis, never the model) is enforced at every layer by the import-boundary test — **now armed and proven non-vacuous** against `ipo.series`.

| Date | Item | Type | Status | Notes |
|---|---|---|---|---|
| 2026-07-19 | **DP-1 — forward subscription recorder** | infra (collection) | **merged `eab1f85`** (+ `1028c14`); built + gated + reviewed. **Ships dark** — inert until deployed and until a real IPO enters its window. **LIVE-PROOF PENDING:** waits for (a) the scp deploy, (b) the first fully-recorded IPO | Piggybacks the `ipo-ingest.service` 30-min fetch — **one fetch, two writes**, no new timer, zero added NSE load. Three plan-first catches, each a defect avoided: **(1)** the blueprint's hook point (after the current-state upsert) **cannot see its data** — `NseSubscription`/`updateTime`/raw response have left scope by then, leaving only the lossy `list[IPORecord]`; capture moved to the fork's **success branch** (`live.py:122`), the *write* still lands after the upsert. **(2)** the **fabricated-row hazard** — `_degrade_subscription` synthesises a replay of the prior record stamped with the prior `captured_at`, so a post-upsert hook would bank rows indistinguishable from genuine ones; capture living only in the `try` makes a degraded reading *structurally* unable to reach the series (a failed fetch banks nothing — an honest gap). **(3)** the sample model had to live **outside `core/`** (inside the scoring guard), hence the new `ipo.series` package. **VM-only by construction:** the sink is created in `run_live_ingest.py`, NOT in `refresh_from_nse` — also the desktop's local-fallback leaf, which would have turned every shipped `.exe` into a recorder during a VM outage. **Store:** one file per IPO, `tmp`+`os.replace`; the load-bearing insight is that a series is **bounded** (~3 days ≈ ~150 samples) so it completes rather than grows — blast radius one IPO, no torn state observable at the live path (~26 MB/yr). Rejected: single-parquet rewrite (v2-A1's — a crash truncates the whole bank, the review's #2/#3), JSONL (a ~4.4 KB sample exceeds `PIPE_BUF`), SQLite. **Schema:** superset of the retired 11 cols incl. `source_update_time`; category set **varies per IPO** (25 vs 22 in fixtures) so `categories` is a variable-length typed list, `noOfShareOffered`/`noOfSharesBid` retained for DP-4, `schema_version` from row one. **No dedupe** (a flat stretch is signal). **Health = option A:** one `Recorder` row fans out to digest + `/status` + DEGRADED + the 20-min alert-check's existing repeat-suppression/recovery — **no second writer** to `alert_state.json`; separates *correctly idle* ("no IPO in window") from *broken*. systemd `OnFailure` **deliberately not overloaded** (its fixed message would contradict the digest). Two self-caught bugs: a **dark-ship violation** (STALE-for-never-run would have painted every VM DEGRADED on merge) and an **unreachable never-shrink guard** (fixed by extracting the `_merge` seam so it defends something real). **Proven:** `MAX\|Δprob\|=0.0` — 386 IPOs, 0 verdict/reason changes, **byte-identical dump** vs `main` (the definitive check, not the guard's proxy); **B1 wall non-vacuous** — importing `ipo.series.store` into `features/build.py` turns the boundary test RED naming `LEAK ipo.series,ipo.series.models,ipo.series.store`, green on revert; torn-write/corrupt-refuses/never-shrink/blast-radius durability; a raising sink still yields records. +24 tests, **541/0**. **Docs:** three on-box realities that were in no repo file — the real unit name `ipo-ingest.timer`/`.service`, the already-applied `context.env` drop-in the runbook still listed as pending, and the undocumented `OnFailure` drop-in — captured as reference copies under `operations/systemd/` (vm-user redacted per the public-repo policy, caught by the pre-merge IP-guard), plus a standing rule written down: **health derives from `ingest_state.json` + live probes, never from systemd unit state.** Console routing lands events in the `engine.log` family (durable/rotated/greppable) but, stated plainly, **not** desktop-console-visible — the recorder runs on the VM and the console reads the local log; a VM `/logs` surface is DP-2/DP-3-shaped follow-on, deliberately not built here. |
| — | **DP-2 — `/subscription-series` read route** | infra (VM) | **NOT STARTED** — blocked on DP-1 | Fourth GET route, one-IPO-scoped (mandatory `?ipo_id=`), `{refreshed_at, ipo_id, samples}` envelope with **per-IPO** freshness. Inherits read-only/rate-limit/no-model proofs; only new work is volume containment + re-checking the shared 60/min limit against DP-3 usage. Honest empty envelope for the (months-long) common case of an IPO with no series. |
| — | **DP-3 — subscription trend graph (V3-9)** | feature (display) | **NOT STARTED** — blocked on DP-2 | The deferred V3-9, now unblocked. Detail-page curve beside "Subscription (final)", terminal aesthetic, per-IPO freshness. **Three distinct honest states:** whole-series-absent (framed empty field — the History-page case), partial-gap (broken line, never interpolated), flat (flat line = signal). Shows the raw book, never a scored/probability-over-time curve (parked #1 / B1). Frontend-only → `MAX\|Δprob\|=0.0`; ships in the `.exe`. |
| — | **DP-4 — close-day trajectory study** | analysis (read-only) | **NOT STARTED** — blocked on accumulated data (~6–12 mo) | Offline study: QIB fill curve by hour + noon-vs-final verdict-divergence counts (stayed-APPLY / crossed-into-APPLY / crossed-down) + the actionable-window verdict. Uses the production scorer/calibrator as-of each reading's own timestamp (no leakage). Produces **findings** that settle the parked #1 — changes no app code. **Measures, does not promote:** a positive result is a *B1 re-gate trigger*, a separate v2-protocol decision, NOT performed here. |

---

## CLOSED / PARKED (carried from the blueprint ledger)

- **B1 (trajectory-as-a-score-feature)** — graveyard; re-gate is a separate deliberate v2 decision, not reopened by v3-DP.
- **Code-review #1 (close-day emission)** — parked, awaits DP-4's measurement; any fix is display-layer, never re-scoring on partial books.
- **DP-1 store physical shape** — open by design, resolved in DP-1's build plan.
