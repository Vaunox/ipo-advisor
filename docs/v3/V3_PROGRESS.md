# V3 PROGRESS LOG

*Maintained per `docs/v3/MASTER_BLUEPRINT_v3.md` Part VIII. One row per item as it moves; newest first. Beyond the original blueprint items (V3-1…V3-11 + bugs), this log now also carries the **emergent second wave** (2026-07-15→16) decided in operation rather than in the plan — the Telegram subsystem, the archive-snapshot revival, the new-IPO trigger, the digest/keepalive tuning, and the VM-primary flip. Statuses distinguish **live-verified** from **merged-but-not-yet-live-proven**.*

> **v3 BUILD-COMPLETE:** **V3-16 (read-only debug console)** — the last open build item — is **built + gated + live-proven** (branch `feat/v3-16-console`, pending operator review + the shipped-`.exe` visual proof). Every v3 build item is now done. Still deferred/parked (genuine future options, not v3 build gaps): V3-9 (subscription trend graph — needs forward interval data), the mypy pin, the HTTP→HTTPS domain+TLS upgrade, and the parked intraday close-day emission diagnosis. See the blueprint's RECONCILIATION STATUS block.

| Date | Item | Type | Outcome | Notes |
|---|---|---|---|---|
| 2026-07-16 | **V3-16 — read-only debug console ("Console log")** — the LAST v3 build item | feature | **built on branch `feat/v3-16-console`; gate green; `/logs` live-proven against the real engine**; pending review + shipped-`.exe` visual proof | **Stage 1** (the honest-logging audit) is already merged (`ca6a6ee`); this is **Stage 2**, the screen. **Engine side:** an in-memory **ring buffer** (redacted at store time via the shared `redacted_payload` — the same scrub the file sink uses, so a token/PAN/auth-header can reach *neither* path; monotonic `seq` + `since` polling; bounded FIFO deque) + the existing **RotatingFileHandler** (5 MB×5 = 25 MB) now with **~14-day expiry** on top of the size cap + a GET-only **`/logs`** (default = ring live-tail with a `last_seq` cursor; `history=true` = the durable rotated files for scroll-back). **Front end:** the **Option-A terminal** overlay the operator chose — "CONSOLE LOG", column headers `ts / level / event / ipo_id / detail` aligned above their columns, INFO green · WARN amber · ERROR red, `fetch_retry ×47` **repeat-suppression** (one counted line, not a flood), **filter** by level + ipo_id/event, **live-tail** (auto-polls the `since` cursor ~2.5 s and appends, `tail -f` auto-scroll that pauses when you scroll up — no "live" label, that word was dropped), **always dark** (a dev tool; ignores the app theme), read-only. A persistent **`UiPrefs` on/off toggle** in Settings → Developer (**default OFF**, durable via the desktop config file like the awaiting-collapse fold) arms the **`` ` ``** shortcut (collision-free vs `g l`/`g u`/`t`/`?`; deadened when off). **Reads the log the engine already writes** — no second logging path. **Off the scoring path:** only `core/logging.py` changed under a protected dir, and it's the guard's allowlisted write-only sink → `MAX\|Δprob\|=0.0` (dump byte-identical vs `main`), guard clean. **Proven:** backend unit+integration tests (ring redaction/seq/eviction, file history, expiry, `/logs` over HTTP, POST→405) + frontend `node --test` (filter/collapse/detail, `devConsole` persistence + desktop restart) + `tsc` + `vite build` + a **live** run — the real engine's `/logs` served `engine_starting`→`scheduler_cycle_*`→`verdict_transition`, `since` incremental, `history` from the file, `POST /logs`→405. pytest/ruff/black/mypy/tsc/node green. **This closes v3.** |
| 2026-07-16 | VM-primary flip (`VM_BASE_URL`) — the shipped app depends on the VM as primary | infra | **merged `454632d`**; wiring + engine-path **live-verified**, but the built `.exe` is the operator's local build (**not yet shipped/proven in a real installer**) | The final infra step. The desktop shell bakes `VM_BASE_URL` into the engine's spawn env, so the shipped `.exe` fetches VM-first and falls back to a direct NSE scrape only when the VM is unreachable (the existing, tested per-cycle fallback). **Build-time injection keeps the IP out of the public repo:** `sidecar.ts::resolveVmBaseUrl` reads env → **gitignored** `vm-config.json` (baked into the `.exe`) → `''` (fail-safe local-only); only `vm-config.example.json` (placeholder) is committed. Live e2e against the real VM: up → both stores `source=vm`; forced-unreachable → `source=local`, bounded, no hang. **Reversible:** `VM_BASE_URL=` empty (no rebuild), or blank the config + rebuild. |
| 2026-07-16 | Keepalive tuned 120s→240s (Oracle idle-reclaim margin) | infra (tuning) | applied on the box + **burn-duration verified** (measured 240s); docs merged `0be8c5f`; **multi-day console P95 confirmation PENDING (operator)** | Reclaim reality corrected vs the blueprint: the shape is **E2.1.Micro = 1 OCPU = 2 vCPU** (a single-thread burn reads ~51% instance CPU, not 100%), and Oracle's rule is **CPU p95 < 20% over a 7-day window** (not "~15%"). 120s was marginal (~20.5% under 5-min aggregation); 240s/30-min = 13.3% duty, ~41%/bucket — comfortable margin at ~7% avg CPU on one `nice`-19 thread. The timer is **box-only config**; only the docstring/runbook sync was a repo change. Verify: console `CpuUtilization` P95, 1-min interval, 7-day ≥ 20%. |
| 2026-07-16 | Digest schedule 2×→4×/day | infra (tuning) | applied (box-only timer) + docs merged `43f2077`; **live** | `ipo-telegram-digest.timer` 00:00 & 18:00 → **00:00 / 06:00 / 12:00 / 18:00 IST**. A clean one-line `OnCalendar` add; nothing assumed the 12-hour gap — the state-change alert-check runs independently every 20 min. |
| 2026-07-16 | New-IPO onset context trigger | feature | **merged `f658948` + deployed**; **live-proof PENDING** — never observed firing on a genuine new IPO (none has appeared since deploy) | Closes the mid-interval onset gap where a new IPO shows empty context for hours. The 30-min NSE ingest diffs the records store's `ipo_id`s pre/post-upsert; a genuinely-new id fires **one** immediate single-symbol context pull, **merge-written** so other IPOs' context and the global `refreshed_at` stay untouched (the one real correctness risk — tested). "New" is derived from `ipo_records.parquet` itself (restart-safe, no separate seen-set file). Dark-ships without `UPSTOX_TOKEN` (present on the box). A firing is captured in journald (~90-day retention), greppable (`new_ipo_context_pull*`). |
| 2026-07-16 | **V3-2 REVISED — VM-write archive snapshot (replaces the desktop-drop)** | infra | **merged `5fc1870` + live-verified** (a push landed both files in `ipo-archive`; idempotent no-op on rerun) | **Supersedes the 2026-07-15 V3-2 desktop-drop→VM-pull design (retired).** LINEAGE (preserved deliberately, not smoothed): desktop-drop built + merged (`0d24237`) → VM-owned Option A → **dropped** (durability-vs-plumbing tradeoff — a two-source cross-check wasn't worth the plumbing for a private tool) → **revived as a VM-write snapshot**: the VM itself pushes `ipo_records.parquet` + `ipo_context.json` daily to `ipo-archive`. The archive deploy key flipped **read-only → write**; the original "a compromised VM can't corrupt the archive" guarantee is **replaced by append-only branch protection** (force-push + deletion blocked) on the now-**public** repo (branch protection is Pro-only for private repos on this account). Single-writer → a durable off-box copy, **not** the two-source cross-check the original design implied — a knowing tradeoff (durability-against-reclaim, not tamper-proofing). Related drop: a **scorecard / faithful point-in-time history** build was considered and **dropped** in favor of backtest-from-what's-re-fetchable (same not-worth-the-plumbing reasoning). |
| 2026-07-16 | VM host/IP scrubbed from the public repo | security/ops | **merged `cb840f5`** + history rewritten (`git filter-repo`) | `ipo-advisor` is public; the real VM IP + SSH user were scrubbed from **all** history (filter-repo — verified zero IP in any commit) and replaced with `<VM_IP>` / `<vm-user>` placeholders. Real values live in a local/private operator note. This underpins the VM-primary flip's build-time injection — the IP never enters public source. |
| 2026-07-15 | **V3-3 alerting realized as a Telegram subsystem (5 slices)** | feature | **merged `d44ac7c` + live on the VM** (digest fires + delivers) | The blueprint's one-line "scrape-failure alerting (email/Telegram/etc)" was built as a full interactive subsystem: a **health digest** (periodic reconciler, now 4×/day), **immediate state-change alerts** (one per edge, no cry-wolf; 20-min alert-check), and **owner-only commands** `/status` (VM health snapshot) + `/login` (record the ≤30-day Oracle console login). Single-source `/` command menu (`setMyCommands`). **3 systemd units** — `ipo-telegram-bot` (long-poll daemon, `Restart=always`), `ipo-telegram-digest.timer`, `ipo-alert-check.timer` — with token/chat-id in `/etc/ipo/telegram.env`; **dark-ships** without creds. Deploy runbook: `operations/README.md`. |
| 2026-07-15 | V3-2/V3-3 two-repo, two-key split | infra (security) | **merged `be53dde`** | The archive key was **read-only** (a compromised VM couldn't corrupt the durable archive — the original V3-2 posture); the heartbeat needs a **write** key, so it got its **own throwaway repo** (blast radius = liveness pings only). A read-only archive key can't coexist with a shared heartbeat-write key. **NB:** the archive key later flipped to **write** for the snapshot revival (2026-07-16, see above) — the read-only posture was traded for append-only branch protection. |
| 2026-07-15 | V3-4 — weekly fallback self-test (proves the local-scrape path hasn't rotted) | feature | **merged `6f393f0`**; **ships dark** — closes the V3-1→V3-4 CODE track (deploy-side: a weekly desktop task) | V3-1 owns the fallback client; this is the periodic **proof it still works**, so the local-scrape code — which runs ONLY when the VM is down — doesn't rot undiscovered until the day you need it. **Drives the REAL path, only the trigger faked:** [fallback_selftest.py](../../scripts/fallback_selftest.py) calls the genuine `refresh_data_plane` with a `VmClient` that raises `VmUnavailable` (exactly as a down VM does), so it takes its `except VmUnavailable` branch → `refresh_from_nse` → `build_live_records` → the real `NseClient` → real NSE → validation → write — no mock of the fallback, no manufactured outage (the VM can be up and serving; the fallback is *selectable*). **Asserts valid data, not "it ran":** `last_attempt_ok=True` + `source=local` + well-formed records with a count; a feed-shape change or broken dependency flips it to FAILED. **Isolated + self-cleaning:** a per-run `TemporaryDirectory` scratch store (never the live `data_store/`) torn down in a `finally` — a weekly run never leaks a scratch dir. **Same polite discipline** as the live ingest (the identical `config.scrape` `NseClient` — one hit a week, no separate unthrottled path) with an isolated throwaway raw cache. **Fails loud in the ritual you run:** the verdict is recorded to `fallback_selftest.json`; `run_heartbeat --fallback-selftest` names it honestly — FAILED → `[STALE] local fallback - self-test FAILED …` (exit 1); the weekly-test-stopped case → `… stale … weekly test not running?` (exit 1, the meta-failure caught); healthy → `[OK] … passed (N records)`. **Ships dark:** purely additive; `run_heartbeat` checks it only with `--fallback-selftest`, no scheduled task → nothing runs; the engine imports nothing new at scoring time (byte-identical; MAX \|Δprob\|=0.0, machine-checked; scoring-path guard clean); `ipo.service.fallback_selftest` sits in the already-fenced service layer. **CI proves both directions** (the important half being the rot signal): canned-valid NSE → `ok=True`; canned-`SourceError` → `ok=False`. +9 tests incl. passes-on-working, fails-on-rotted, cleans-up-even-on-failure, surfacing (failed/stale/never-run/fresh), persistence — plus a **live** demo (working → `[OK] passed (1 records)`, exit 0; rotted → `[STALE] FAILED`, exit 1). Shared the `ago` humanizer into `ipo.service.heartbeat` (one definition, V3-3 + V3-4). pytest/ruff/mypy/black green. **Deploy-side (operator):** a WEEKLY desktop scheduled task + `--fallback-selftest` in the ritual. |
| 2026-07-15 | V3-3 — VM monitoring: heartbeat-staleness detector + out-of-band liveness ping + Oracle keepalive | feature | **merged `18df49e`**; **ships dark**. Alerting was realized as the **Telegram subsystem** (2026-07-15, `d44ac7c`); keepalive later **tuned 120→240s** (2026-07-16, `0be8c5f`) — see rows above | Keeps the VM data plane visible and alive, via two independent, dark-shipping mechanisms. **Primary — heartbeat-staleness detector (always on, can't-itself-fail):** the VM writes a small `heartbeat.json` (beat time, ingest freshness from the BUG-1 `ingest_state`, free disk, keepalive marker) each cycle and pushes it to a git channel; the desktop's `run_heartbeat --vm-heartbeat` reads it and **names every failure honestly** — `VM heartbeat - last beat 1d ago — fetch may have stopped`, `VM ingest - NSE fetch failing …`, `VM disk - 4% free …`, `VM keepalive - … Oracle reclaim risk` — never a generic "unhealthy", and **exits non-zero**. Needs *nothing alive on the VM* at check time and *no VM credentials on the desktop* (a git-history read), so it can't fail the way a monitor-on-the-box would; it carries essentially every failure mode. **Backup — one out-of-band liveness ping:** for the single unrecoverable, time-sensitive case (Oracle reclaiming the instance — a reclaimed VM doesn't come back), [vm_heartbeat.py](../../scripts/vm_heartbeat.py) pings a free dead-man's-switch (`HC_PING_URL`, healthchecks.io) each cycle; the monitor emails when pings *stop*. One liveness bit, not an alerting stack. **Keepalive (load-bearing):** Oracle reclaims Always-Free compute that AVERAGES idle over a window (a brief spike doesn't help), so [vm_keepalive.py](../../scripts/vm_keepalive.py) runs a bounded, hard-capped (≤600s) CPU burn and touches a marker the heartbeat checks — a *dead keepalive is itself a fail-loud signal* before reclamation. **Ships dark & downstream:** purely additive — the engine imports nothing new at scoring time (byte-identical; MAX \|Δprob\|=0.0, machine-checked; scoring-path guard clean); the detector is inert without `--vm-heartbeat`, the ping without `HC_PING_URL`, the writer/keepalive without their systemd timers. `ipo.service.vm_health` sits in the already-fenced service layer (the transitive import guard keeps it off the scoring path). **Built + tested now:** `ipo.service.vm_health` (model + honest-naming detector + read + ping), the two scripts, and the `run_heartbeat --vm-heartbeat` integration; 13 tests incl. the headline **fires-on-stale / quiet-on-fresh** proof, honest-naming per dimension, corrupt→stale, dark-ship ping, writer-reflects-`ingest_state`, keepalive-bounded — plus a **live** demo (fresh → all VM lines OK, exit 0; stale → `VM heartbeat STALE` + `ATTENTION DEGRADED`, exit 1). pytest/ruff/mypy/black green. **Deploy-side (operator, per runbook):** the heartbeat git channel (a `heartbeat` branch of the archive repo), the healthchecks ping URL, the VM systemd timers (heartbeat + keepalive), keepalive tuning verified against the Oracle console CPU metric, and a **HARD ≤30-day Oracle-console-login calendar item** (no script can prevent account-level reclaim). |
| 2026-07-15 | V3-2 — durable archive (app history → private git rendezvous → VM append-merge) | feature | merged `0d24237`, then **SUPERSEDED 2026-07-16 by the VM-write snapshot (`5fc1870`)** — this desktop-drop→VM-pull design is **retired**; the lineage + reasoning are preserved in the revised row above | *(Historical — the design as originally built, now replaced. Kept for the decision trail; see the 2026-07-16 "V3-2 REVISED" row for why the desktop-drop is dead and the VM writes the snapshot instead.)* The **output half** of the data plane (V3-1 was the input half): the app's one non-reproducible artifact — `verdict_transitions.json`, *when* each verdict changed — is backed up so it outlives the local machine, **without** giving the app any write access to the durable copy. Shape: a daily Windows scheduled task ([archive_drop.ps1](../../scripts/archive_drop.ps1)) **drops** the file outbound (git push) to a **private git rendezvous** (a separate repo — operational data, deliberately NOT a branch of the code repo); a daily VM systemd timer **pulls → validates → append-merges** it into a durable archive the app can never touch ([archive_pull.py](../../scripts/archive_pull.py) over [ipo.archive](../../src/ipo/archive)). **Append-only, not mirror:** the merge is a union keyed on the whole transition, so it is idempotent + order-independent — a missed / doubled / out-of-order pull all converge, and a partial or lost local copy can only add, never delete (the same never-prune-the-log discipline as BUG 2). **Don't trust the drop:** a malformed/truncated rendezvous file is REJECTED whole (exit 1, nothing merged) — the archive's integrity never depends on the drop being well-formed (V3-1's "don't trust a 200", on the pull side); a leading UTF-8 BOM is tolerated at the read boundary (`utf-8-sig`). **Ships dark:** purely additive — the engine imports nothing from `ipo.archive` (byte-identical; MAX \|Δprob\|=0.0 by construction, machine-checked: 0 changes under the scoring path), and the dropper is a silent no-op with no rendezvous configured — inert until deployed. **Strictly downstream of verdicts:** written *from* transitions, never read into the feature vector — proven by the import-boundary guard (extended so `ipo.archive` is unreachable from features/model/calibration/core, alongside the context store) + a direct-reference scan. **Read-API untouched** (still GET-only); **credentials are deploy-side** (the SSH deploy key lives only in the scheduled task's + the VM's environment, in the operations runbook, never committed). +tests: validation (rejects non-JSON / truncated / bad-row / non-array; tolerates BOM), merge (dedupe, idempotent double-pull, order-independent out-of-order pull, append-only-never-deletes), store round-trip + corrupt-raises, pull-merge end-to-end + rejection-leaves-archive-untouched, boundary guard; the PowerShell dropper smoke-verified locally (drop→push, idempotent, dark-ship no-op — not in CI, which is ubuntu). pytest/ruff/mypy/black green; full loop verified end-to-end. **Deploy-side (operator, per runbook):** create the private repo, set the deploy key, schedule the local drop + the VM pull. |
| 2026-07-15 | V3-1 — VM data plane (app side): read-API + client + honest fallback + source indicator | feature | **shipped in 3 steps** (tags `v3-1-vm-read-api` / `v3-1-vm-client` / `v3-1-fallback-chip`); shipped dark, then **flipped to VM-primary-by-default in the build** (2026-07-16, `454632d`) — the app now depends on the VM as primary; see the flip row above. VM side later completed (records timer `run_live_ingest.py` `cb04947`; context timer live `a1a4675`) | The VM data plane's **app side**, merged in three steps. **Step 1** (`v3-1-vm-read-api`): a GET-only FastAPI read-API ([vm/server.py](../../src/ipo/vm/server.py)) — `/health`, `/records`, `/context`, each a `{refreshed_at, data}` envelope so freshness travels *with* the data; no mutation route and no engine import, so the VM **structurally cannot run the model or be made to act** (BUG 1's door, re-refused). **Step 2** (`v3-1-vm-client`): `VmClient` (10s timeout, 2 retries) + a per-cycle **VM-primary / honest-local-fallback** refresh ([data/ingest/data_plane.py](../../src/ipo/data/ingest/data_plane.py)) — records fall back to a fresh NSE re-scrape (`source=local`), context keeps the last-known cache and **ages honestly** (`context_source=local`) because the Upstox token lives on the VM; each store's provenance is recorded on `/status` (extends BUG 1's `IngestState`). *Don't trust a 200* — the envelope is schema-validated and a malformed/truncated body triggers fallback (tested). **Step 3** (`v3-1-fallback-chip`): the source state folded into the **ONE** SyncStatus chip (never a second, contradicting chip) — quiet when healthy; on a store fallback it appends the honest per-store split (records fresh vs context aging) and turns amber, flagging **only what the `Updated …` timestamp can't already convey**. **Ships dark:** with no `VM_BASE_URL` (`vm_client=None`) the engine behaves exactly as before the VM existed — pure local scrape, `context_source` cleared to `null`, no chip — so this merges **inert** until the VM is deployed. **Enable** by setting `VM_BASE_URL` (read directly by the runner — deliberately NOT an `IPO_*` key, so VM wiring can't leak into `AppConfig`). All three states **live-verified against a real `/status`**: both-VM (both stores pulled over real HTTP from `ipo.vm.server`), dark-ship (no chip), VM-down (amber split). Step 3 **frontend-only** — MAX \|Δprob\|=0.0 by construction (machine-checked: 0 Python in the diff) + scoring-path guard OK; steps 1–2 scoring-path untouched (git-proven + guard). The **output half** (app history → durable VM archive) is deliberately NOT here — that's **V3-2**, and by design the **VM pulls** (the app never pushes; the read-API stays GET-only). +tests: VM server + envelope validation + malformed-fallback + dark-ship no-op (pytest), `fallbackStatus` 3-state (`node --test`, 18/18); pytest/ruff/mypy/tsc green. |
| 2026-07-14 | V3-10 — anchor investor list (context only) | feature | **DROPPED — no probes; even perfect data changes nothing** | **No Upstox source, and no reason to seek another.** Upstox's IPO API has exactly two documented endpoints (*Get IPOs*, *Get IPO Details*); the details response is the same 26 fields already mapped (registrar / rhp_url / lot_size / isin / industry / cut_off_price / status / timeline / …) with **no anchor field** — there is no anchor route to probe. **Do not re-check.** Chittorgarh *does* carry anchor lists, but V3-10 was dropped **without diagnosing them** because even perfect data would not change the call: **(1) Redundant with QIB.** `anchor_quality` was tested as a model feature and deferred (data-limited) with a strong QIB-redundancy prior — anchors who commit pre-open also bid, so their conviction is already inside the QIB subscription number; it is not new information, just *earlier* information. **(2) Superseded at the decision clock.** The anchor list is a *pre-open* sentiment signal, but our verdict is delivered at **close of bidding**, when the full book is in — by which point the anchor list is stale and irrelevant (a guess at the very thing we then measure directly). **(3) The only feature that would need a NEW permanent dependency.** Every other v3 feature came free off NSE or Upstox; V3-10 alone would require a permanent Chittorgarh scraper the VM must run and keep alive forever (breaking on every layout change) — for a display-only nicety. **Do not re-propose.** |
| 2026-07-14 | Finding-④ — listing-resolution **strand detector** (Option A; BUG-1-class silent failure) | bug (correctness) | built (branch `feat/v3-11-context-fields`, pending review) | Surfaced by the `status` cross-check thread and diagnosed first. The Live→History resolution (`resolve_listings`) can fail with **no signal**: **Mode 1** — the book closes but NSE past-issues never yields a matching symbol, so `listing_date` is never stamped; **Mode 2** — `listing_date` is stamped but the bhavcopy price never backfills past `PRICE_BACKFILL_DAYS`. Either way the IPO sits forever under an honest-looking "awaiting listing" label. **The path itself works** — traced end-to-end + a new integration test (awaiting → `resolve_listings` → leaves the awaiting surface → in History with the correct net return + HIT/MISS), and the dev boot pull was observed resolving a stale `knack` fixture live — and **the real store is currently clean** (0 strands; Kusumgar/Laser Power are legitimately inside T+3, not stranded). The defect was that **failure is silent** (the BUG-1 lesson: a path that can fail while looking fine). Fix (**Option A**, self-contained — our own dates only, no second source): one predicate `listing_overdue_state` ([service/lifecycle.py](../../src/ipo/service/lifecycle.py)) covering **both** modes, with a **holiday-aware trading-day grace buffer** (T+3 + 2 trading days via the NSE calendar) so a legit one-day slip doesn't cry wolf — the same "flag a real departure, not noise" discipline as the drift monitor's Wilson trigger. Surfaced in **two** places: a `listing_overdue` board flag → History's awaiting section names the strand honestly ("listing overdue — resolution may have failed" / "listing outcome overdue — price never recorded") with an overdue **count + red badge**; and `run_heartbeat.py --data-dir` prints an `[OVERDUE]` block and **exits non-zero**, so a strand appears in the operator ritual, not weeks later. **Option B (Upstox `status` drift-flag) considered, NOT built** — its only marginal gain over A is distinguishing "we missed it" from "listing genuinely delayed": real but low-value for a single-operator app, and it adds a dependency on a manually-refreshed cache, whereas A works even if Upstox/the cache is down. **Do not re-propose B.** Scoring path untouched (git-proven; MAX \|Δprob\|=0.0 across 386). +tests: lifecycle classifier (both modes, buffer, holiday-aware, real parquet store), board overdue flag, `awaitingLabel` via `node --test`; heartbeat smoke-verified (loud, exit 1); UI verified live (badge + honest label, no console errors). |
| 2026-07-14 | V3-11 — minor context fields (`isin`, `industry`) + `status` / `cut_off_price` decisions | feature | `isin`+`industry` built; `status`(B) & `cut_off_price` **considered-not-built** (branch `feat/v3-11-context-fields`) | `isin` + `industry` shipped as plain display metadata through the SAME per-IPO context store (one shared `field_state` rule, **no source named**, honest degradation) — a compact "Reference" card on the detail page, verified live. **`cut_off_price` NOT built** — our `issue_price` **is** `price_band_high` ([core/types.py](../../src/ipo/core/types.py)) and Upstox `cut_off_price` == band high in every sample; the detail page already renders the band, so it would re-display a value we already hold → doesn't earn its place. **`status` NOT built as a display field** — the diagnosis showed it is too coarse to arbitrate our lifecycle (the whole 394-IPO snapshot is `{closed, listed}`) and our derived status is pure date arithmetic (correct when the dates are right); the real gap it *hinted* at — silent resolution strands — is fixed directly by **finding-④** instead of by importing an external status. Also: pinned `mypy>=1.9,<2` — the unbounded pin let mypy 2.x (2.1.0) silently redden the CI gate on `main` (stricter bare-generic checks) with no code change; fixed the two affected files' annotations and capped the version (a dependency that can break your own gate is itself the silent-failure class). |
| 2026-07-14 | V3-8 — `lot_size` (Upstox **sole source** — NOT a fallback) | feature | merged to `main`, tag `v3-8-lot-size` | **Reframed by the diagnosis** (renamed from "lot_size fallback"): NSE provides `lot_size` on **0% of IPOs** — its `ipo-current-issue`/`all-upcoming-issues` endpoints carry no lot field, and the backfill CSV column is 0/386. So there is **no NSE value to be authoritative anywhere** — the blueprint's "NSE remains authoritative where it has a value" rule is a **dead requirement** (drop it) and Upstox is the sole source for 100% of IPOs. **The agreement check (NSE-vs-Upstox) was impossible** (zero NSE values), so it was correctly substituted with a **regulatory-invariant** corroboration: SEBI's ~₹14–15k minimum retail application means a correct `lot × cut-off` must land there — **22/22 sampled Upstox lots do** (₹13,420–15,000, tightly clustered at ₹14.6–15k, exactly what issuer-chosen lots look like). **Sufficient** because `lot_size` is **static** (fixed at RHP filing — none of the stale-snapshot failure mode that sank `total_subscription` in Check-1) and Upstox reads it from the same RHP field the exchange uses; an exact second-source (Chittorgarh) check was **initially judged not warranted** (the app places no bids, so the broker enforces the true lot at application time — an off-by-one is a ~₹200 planning delta, not an invalid bid). **Reversed by operator request and run post-hoc (2026-07-14): 20/20 EXACT agreement** — Chittorgarh's exact integer lot equals the shipped Upstox lot for every one of the 20 mainboard IPOs (REIT `BAGMANE` + InvIT `RIIT` excluded: trust *units*, not a like-for-like share lot), each application amount inside the SEBI ₹13,420–15,000 band. The regulatory-invariant corroboration is thereby **upgraded from "the application amount is right" to an exact confirmation of the integer itself** — zero disagreements, nothing to correct. Evidence: `research/chittorgarh_lot_crosscheck.json` (URLs discovered from Chittorgarh's rendered 2026 mainboard list; lot read from each detail page's "Retail (Min)" row). Flows through the **same context store** (`IpoContext.lot_size`, `refresh_context.py` captures it, one `field_state` rule, `GET /context/{id}`). **UI (operator-set):** no source attribution (no "via Upstox"); shown as an **indicative** figure — "Lot (indicative): ≈ 70 shares · approx ₹14,980" — so a possibly-imprecise value never wears an authoritative face. Scoring path untouched (MAX \|Δprob\|=0.0); pytest 308, ruff/mypy/tsc green; verified live (indicative lot renders, no provider named, no console errors). |
| 2026-07-14 | V3-5 — RHP/DRHP direct links | feature | merged to `main`, tag `v3-5-rhp-links` | Data-shape diagnosed first (operator-run probe): valid Upstox pre-close statuses are `upcoming`/`open`; `rhp_url` is fully populated from `open` onward (open/closed/listed 100%, real SEBI/issuer URLs), absent on `upcoming`. **DRHP dropped (RHP-only):** `drhp_url` = 1/28, and the lone case is an `upcoming` IPO with symbol `'NONE'` — **unjoinable even where it exists**, so it's a non-field, not sparse data. **Generalized the V3-6 registrar cache into ONE per-IPO Upstox context cache** (Option A) — `registrar_info.json`→`ipo_context.json`, `AllotmentStore`→`ContextStore`, `refresh_allotment.py`→`refresh_context.py` (queries open+closed+listed, excludes `'NONE'`-symbol at ingest), one `field_state` staleness rule shared by registrar + RHP + future fields (V3-8/11/10 add a field, not a path). New `GET /context/{id}` serves the RHP + its state; Detail page shows it labelled **"Red Herring Prospectus (RHP)"** (never generic, never the draft). **RHP-open security:** the openExternal allowlist gains the SEBI filing host (`sebi.gov.in`) → one-click for SEBI-filed RHPs; issuer-hosted RHPs render inert copyable text (unbounded domains, can't vouch). Requirements met: transitive guard generalized to "no `ipo.service.*` reachable from the scoring path" (rename-proof); one staleness rule; VM-runnable refresh; symbol-keyed join with `'NONE'` excluded at ingest; allotment surface unchanged (verified live + MAX \|Δprob\|=0.0). +tests (allowlist SEBI/issuer, `build_ipo_context` RHP states, `/context` endpoint); pytest/ruff/mypy/tsc/node --test green; verified live (RHP button → SEBI, allotment deep-link unchanged, no console errors). |
| 2026-07-14 | V3-6 — Allotment tab | feature | merged to `main`, tag `v3-6-allotment-tab` | Data-shape diagnosed first (operator-run probe): `registrar_info` is real, 100% populated across the sample, trustworthy values (MUFG Intime / Bigshare / KFin with their real portals) — GREEN, unlike Check-1's stale `total_subscription`. New top-level tab (Live → Upcoming → **Allotment** → History) listing IPOs at/past the allotment stage (closed + awaiting + listed ≤7 days, via `status.ts` scope) with registrar name, a **deep-link to the registrar's own allotment-check page**, and a collapsible grievance contact. **Zero PAN** — the app links out (new `shell:openExternal` IPC, https-validated); the user enters PAN on the registrar's site. **Option B data path (VM-discipline):** the app only READS a token-free cache in the data plane (`<data_dir>/allotment/registrar_info.json`); the fetch is an external, VM-runnable script (`scripts/refresh_allotment.py` — pure/portable, env token, `--data-dir`), so when the VM lands it becomes VM-primary/local-fallback with every other feed (one pattern, trivial swap). **Three structural guarantees:** (1) registrar data physically can't reach the model — own store/type, never an `IPORecord` field; proven by `test_scoring_path_cannot_reach_registrar_data` (permanent import-graph guard); (2) degrades honestly (no cache → `available=false`; per-IPO no entry → `registrar=null`; freshness line); (3) severable (read-only, off the scheduler — Upstox down never touches verdicts). Scoring path untouched three ways: git-proven, import-graph proven, **MAX \|Δprob\|=0.0 across 386 IPOs**. +15 tests (297→312); pytest/ruff/mypy/tsc green; verified live (tab renders, join works, deep-link external not in-app, no PAN input, no console errors). *Carry-forward for V3-5:* re-run the probe on `upcoming`/`open` to check `drhp_url` there (0/22 on closed/listed is expected — superseded by RHP) before finalizing "show whichever document exists." |
| 2026-07-14 | V3-7 — `mandate_end_date` → replace the approximated close-day cutoff | feature (correctness check) | **NOT ADOPTED — sourced value changes nothing** | Diagnosis-first, and the blueprint's premise was refuted (3rd time this run). The premise: an "approximated SEBI cutoff" controls *when* the book-closed scoring cycle fires, and a sourced `mandate_end_date` would improve it. Reality: **`book_closed = asof_date >= close_date`** is **date-level** ([features/build.py](../../src/ipo/features/build.py)) — it flips at midnight of the close day, not at any cutoff time. The `_CLOSE_EOD_HOUR = 18` constant ([service/engine.py](../../src/ipo/service/engine.py) `decision_asof`) is **only a date-anchoring device** — it freezes the as-of clock at `close_date @ 18:00` so the decision snapshot stays pinned to the close day. **`asof.time()` is never read anywhere** in `features/`, `engine.py`, or `model/` (grep-verified); every feature is date-level (book_closed, GMP [None live], regime, subscription off the record). So the cutoff *hour* has **zero** effect on any feature/probability/verdict, and replacing it with a sourced `mandate_end_date` (17:00/17:30/whatever) would change nothing. Field not verified — and deliberately not: Check-1's Upstox pull never captured `mandate_end_date` (only `total_subscription`/`status`), and verifying a field that cannot matter is wasted effort (esp. given Check-1's endpoint-wide staleness). **Do not re-chase:** `_CLOSE_EOD_HOUR` is date-anchoring only; its exact minute is immaterial by construction. No code touched; research branch deleted. **Reconfirmed 2026-07-14 by the V3-6 probe** — `mandate_end_date` is not merely "changes nothing," it is **semantically not a close-day cutoff at all**: for an IPO listing 2026-07-16 the field reads `2026-08-24` (the ASBA bank-mandate *validity* end, ~a month after listing). The real bidding close is `timeline.application_end_date`. Had we adopted `mandate_end_date` as the cutoff we'd have wired in a nonsense date. Do not wire it as a close cutoff. |
| 2026-07-14 | BUG 3 — theme toggle breaks after changing theme in Settings | bug | fixed (merged to `main`, tag `bug-3-theme-toggle`) | Diagnosis-first — and the empirical check **refuted** the initial hypothesis (as BUG 1's did). Suspected two-writers-without-a-shared-store; in fact both writers already route through the shared `setThemeMode`→`prefs.ts`. Real defect: stale **read-side** caches — the header `ThemeToggle` (`useState(resolved)`) and Settings (`useState(theme)`) each held a private copy updated only by their own clicks, so after an external change (Settings, or the `t` shortcut) the toggle acted on a stale value → the **first click was swallowed** (off-by-one). Reproduced live before fixing. Fix: made `prefs.ts` a small subscribable store (`subscribe`/`notify` on every persist) + a `useThemeMode()` = `useSyncExternalStore(subscribe, getThemeMode)`; deleted both private caches so the toggle, Settings, and the (already-correct) `t` shortcut agree by construction. Ruled out (didn't fit evidence to theory): hydration ordering (main.tsx awaits hydrate→applyTheme→mount), CSS-class leak (setAttribute replace), system-race (see known limitation below). Scope held to theme only — a scan found the other five prefs caches (density/notifications/costs/startup/pinned) share the shape but are **single-writer**, so latent-only; a caveat comment at the `prefs.ts` getters warns to convert them if a 2nd writer is ever added. GATE BUG-3: `prefs.test.ts` (store core, DOM-shimmed) via `node --test` — 4/4. PWA-only: scoring path untouched by construction; tsc + node --test (10/10) green; verified live — Settings→toggle **and** toggle→Settings **and** `t` shortcut all consistent, no swallowed click, no console errors. |
| 2026-07-14 | BUG 2 — alerts accumulate without limit | bug | fixed (merged to `main`, tag `bug-2-alert-retention`) | Diagnosis-first. Root cause: the alert center's "Recent APPLY crossings" rendered the entire append-only transition log with no relevance scoping or per-IPO dedupe; the client `alertsSeen`/`notifiedCrossings` sets also grew unbounded. **Not** the BUG 3 two-writers shape — the alert data is single-sourced (the transition log) and each client set has one writer tracking a different thing; this is an absence-of-retention failure. The log is load-bearing (detail-view per-IPO history + scheduler priming), so the fix scopes the **view**, never prunes the store. Retention rule (operator-adjusted): keep while the outcome is UNRESOLVED — Open/Closes-Today/**Awaiting-listing** all kept, only **Listed** drops (coherent with the V3-6 Allotment tab; Awaiting is a bounded ~T+3 window). Extracted `statusLabel` + new `alertRelevant`/`isListed` into shared `status.ts` (killed a third drifting copy in History.tsx); pure selection logic in `alerts.ts` used by both the alert center and native notifications (tested logic == shipped). GATE BUG-2: `alerts.test.ts` (multi-IPO cycle) via `node --test` — 6/6. PWA-only: scoring path untouched by construction (no engine/Python/model/config files); pytest 286 green, tsc + node --test green; verified live (alert center renders, awaiting-listing retained, no console errors). |
| 2026-07-14 | BUG 1 — stale verdicts under a fresh-looking timestamp | bug | fixed (merged to `main`, tag `bug-1-freshness-truth`) | Diagnosis-first. Defect 2 (freshness truth): the chip was bound to react-query `dataUpdatedAt` (local API-answer time) while `refresh_from_nse` never surfaced failure — structurally unable to report staleness. Added `IngestStateStore` + `GET /status`; `last_success` advances only on a confirmed NSE pull, persisted, failures visible ("… · retrying"). Defect 1 (on-open refresh): nothing client-side could trigger a real pull (empirically confirmed — 29 `/board` reads → 0 NSE fetches; only the scheduler timer/boot pull). Shell-owned trigger over a parent-only stdin channel on window focus/restore + Refresh button; engine-side debounce (~15 s). Renderer stays read-only, API stays GET-only. Scoring path byte-identical: MAX \|Δprob\|=0.0 across 386 IPOs, git-proven (nothing under model/calibration/features/core, models/, config/). +12 tests; ruff/mypy/tsc green; verified live (green chip) + stdin-channel harness (debounce + real pull). |

---

## Known minor limitations (documented, not bugs to chase)

- **Theme `system` mode does not track runtime OS-theme changes.** With theme set to *System*, the app resolves light/dark once (via `matchMedia('(prefers-color-scheme: dark)')`) at load / on each explicit theme change, but there is **no live `matchMedia` change-listener**, so flipping the OS between light and dark while the app is open does not re-theme it until the next reload or manual theme change. Surfaced during the BUG 3 diagnosis (2026-07-14) and deliberately **not** bundled into that fix — it is a separate, latent behavior, not the toggle desync. If it ever becomes annoying it is a one-line fix: add a `matchMedia(...).addEventListener('change', …)` that re-applies the theme while mode is `system`. Recorded here so it is a known behavior rather than a future mystery bug.
