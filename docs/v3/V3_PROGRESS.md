# V3 PROGRESS LOG

*Maintained per `docs/v3/MASTER_BLUEPRINT_v3.md` Part VIII. One row per item as it moves; newest first. Beyond the original blueprint items (V3-1…V3-11 + bugs), this log now also carries the **emergent second wave** (2026-07-15→16) decided in operation rather than in the plan — the Telegram subsystem, the archive-snapshot revival, the new-IPO trigger, the digest/keepalive tuning, and the VM-primary flip. Statuses distinguish **live-verified** from **merged-but-not-yet-live-proven**.*

> **v3 BUILD-COMPLETE:** **V3-16 (read-only debug console)** — the last open build item — is **merged `1693e48`** (2026-07-16), reviewed, and running live in the shipped `.exe`. Every v3 build item is now done and on `main`. Still deferred/parked (genuine future options, not v3 build gaps): V3-9 (subscription trend graph — needs forward interval data), the mypy pin, the HTTP→HTTPS domain+TLS upgrade, and the parked intraday close-day emission diagnosis. See the blueprint's RECONCILIATION STATUS block.

| Date | Item | Type | Outcome | Notes |
|---|---|---|---|---|
| 2026-07-23 | F8(b) — server-side min-level filtering + open on a useful window | bug (console/engine) | **merged `1245e66`** (fix `6d9b812`); **code-proven headless + operator-approved via preview; not yet `.exe`-verified** | The console couldn't surface older problems — two symptoms, one root (the initial load reads only the level-blind, per-process ring). (1) Selecting a level filtered only the loaded window: `/logs` had no level param, so an error older than the INFO-dominated newest-1000 ring window was never fetched until hand-scrolled (measured: a full ring is INFO-only). (2) A freshly-started engine showed only the current session: its ring starts empty and fills ~1 cycle/30min (measured: 7 lines in ring vs 569 on disk). Fix: threaded a `level` param through `GET /logs` → `RingBufferHandler.entries()` → `file_history` with MIN-level semantics (`warn+` keeps WARN and ERROR — exact-bucket was HIDING errors, the same "UI must never lie" failure as F8c); `latest_seq` stays global so polling stays incremental; `level_rank`/`min_rank_for` mirror the client's `levelClass`. Chips relabelled `all · warn+ · error` (dropped the now-redundant `info` — under min-level "info and above" == "all", a chip that would do nothing). `filterEntries` is min-level too (instant client feedback + defence-in-depth). `level` is now a FETCH dimension: a `[level]` re-pull effect with adaptive disk-backfill when the ring is thin (< 200), so the console OPENS with real history instead of only the current process's lines; the poll, `loadOlder`, and the F8a restart re-pull all thread the level; a `levelGenRef` fences out in-flight fetches from a prior level; a pure `resetForLevel()` owns exactly what resets (`query`/`restoreRef`/`openKey` deliberately untouched — an expanded ERROR row stays open into `warn+`). Touches the ENGINE (Python) as well as the client — the only console fix that does. LOGGED FOLLOW-UP (flagged in a comment, not hidden): `file_history` re-reads whole rotated files per page and the min-level filter defeats its early `break` for rare levels, so a rare-level page may scan ALL rotated files (~25 MB); acceptable for an on-demand dev tool, the proper fix (streaming reads + a bounded scan, or a level-partitioned index) is a follow-up. Proof: pytest (server min-level on ring+disk, ranking, global cursor) + node --test (`resetForLevel` HARD FENCE — every reset field asserted AND `query`/`restoreRef`/`openKey` asserted structurally ABSENT; min-level `filterEntries`; `collapse` folds warn/err made adjacent by the filter). Live-proven via a thin-ring repro engine: opens a 506-row window (not 7), `warn+` surfaces 9 old disk warn/err (6 WARN + 3 ERR, no INFO), `error` folds to ×3, all→warn+→error→all clean with no dup. Gate: PWA tsc + 107 node --test + vite build; mypy 0 / ruff / black / pytest 608; desktop untouched. Off the scoring path (guard OK, MAX \|Δprob\|=0.0). |
| 2026-07-23 | F8(a) — restart-cursor un-freeze (the live tail silently froze after an engine restart) | bug (console) | **merged `65a5dbe`** (fix `743fc6c`); **code-proven + live-verified against a real seq-regression; not yet `.exe`-verified** | The ring's `seq` cursor is per-process, so an engine restart reset `seq` to 1 while the client kept polling `since=<old high>` → `ring_tail` returned zero rows forever: the live tail froze SILENTLY (no error, no cue), recoverable only by closing and reopening the console (the initial-load effect had `[]` deps, resetting the cursor only on mount; `d46cf62` had removed the ↻ that used to recover it). Fix: a pure `shouldResetCursor(lastSeq, since) = lastSeq < since`; when the reported `last_seq` is strictly below the cursor the ring regressed → the engine restarted → the poll resets `sinceRef=0` and re-pulls the fresh tail, appending the post-restart lines. No duplication — the re-pulled tail is post-restart (strictly newer ts, fresh seqs), disjoint from what's held (`prependOlder`/`entryKey` dedup is for the ring→DISK seam; this is ring→ring across a process boundary). Can't loop — the reset re-syncs `sinceRef` to the live ring's `last_seq` (monotonic within a process), so the next poll is `last_seq >= since`; at most one reset per restart. Scroll-back untouched — only the ring cursor resets; `oldestTsRef`/`historyDoneRef` are ts-based and the rotated files persist. No synthetic restart marker — the engine's own boot lines (`engine_starting`/`scheduler_loop_started`) resume in the tail and mark it honestly. Narrow edge noted (out of spec, NOT fixed): if a new ring's `last_seq` coincidentally exceeds the stale cursor (>2000 lines in one 2.5s poll — implausible), no reset fires and a few early boot lines are skipped, a small disk-recoverable GAP, never a dup or a freeze. Proof: node --test (normal poll / regression / first-poll-zero-state / anti-loop bound) + a live `/__restart` repro engine — a frozen tail (40 rows, cursor at seq 40) resumed to 55 after a seq-regression to last_seq=15, no dup, no loop, scroll-back intact (55→448 chronological); the `/__restart` debug endpoint was a scratchpad throwaway, confirmed absent from the repo. PWA-only. Off the scoring path (guard OK, MAX \|Δprob\|=0.0). |
| 2026-07-23 | F7 — clamp/expand/copy long console lines (a 165-char line dead-ended at `atte…`) | bug (console/shell) | **merged `40315eb`** (fix `e0e9b1a`); **code-proven + operator-approved via a dev gallery; not yet `.exe`-verified** | The detail cell clipped with `nowrap` + ellipsis, and `.cl-body{overflow-x:auto}` was INERT (the grid's last track is `minmax(0,1fr)`, so nothing overflowed the row to scroll to) — a 165-char `stdin_refresh_outcome` line dead-ended at `atte…` and the operator's workaround was opening `engine.log` directly. There is NO truncation anywhere in the data path — `exc_info` tracebacks are unbounded (1000s of chars), so no fixed width could ever suffice; wrapping is the only approach. Fix: `.dt` now wraps, clamped to 4 lines by default (measured: at the 408px cell, 54 chars/line → 3 lines = 162 clips that very line by ~3 chars, 4 lines = 216 shows it whole); click the row to expand to full with `white-space:pre-wrap` preserving the traceback's embedded newlines and `overflow-wrap:anywhere` breaking unbreakable tokens; the click toggle is guarded on an active text selection (a drag-select never toggles) and keyed on the row's stable F8cd key so a prepend can't move it. A copy button on the expanded row copies the full line via a NEW write-only + string-only `clipboard:write` preload bridge (`window.ipoDesktop.copyText` → `ipcMain` → `clipboard.writeText`, reliable in the `file://` renderer where `navigator.clipboard` is not), with a `navigator.clipboard` browser fallback. Touches DESKTOP (preload + main) as well as PWA — the only PWA console fix that does. Proof: node --test (`formatDetail` keeps a multi-line `exc_info` intact — the clamp is visual-only, the data stays whole); a dev gallery `pwa/dev/console-gallery.{html,ts}` (chip-gallery pattern, proven NOT in `dist/`) shows 3- vs 4-line clamp on the three real worst cases + interactive expand/copy, fixture host is a PLACEHOLDER never the real VM IP; operator-approved via the preview; copy proven end-to-end (the button passes the full 424-char detail with 7 newlines; Electron `clipboard.writeText`→`readText` round-trips it exactly). The `clipboard:write` IPC only writes a string and returns void — it can't read/exec/navigate, consistent with the shell lockdown. Gate: PWA tsc + 103 node --test + vite build; desktop tsc + 26; mypy 0 / pytest 603. Off the scoring path (guard OK, MAX \|Δprob\|=0.0). |
| 2026-07-22 | F8(c) + F8(d) — collision-proof console keys (filter "did nothing"; rows duplicated + out of order) | bug (console) | **merged `461bb44`** (fix `97befd0`); **code-proven headless (3 fences) + dev-verified in the field-failure scenario; not yet `.exe`-verified** | `collapse` keyed rows `${entry.seq ?? rows.length}-${message}`, so disk rows (no `seq`) fell back to the array INDEX — which collides with any ring row whose `seq` equals that index, and shifts on every prepend. React's contract on duplicate keys — "children may be duplicated and/or omitted" — produced BOTH reported defects at once: **F8c** the level filter appeared not to work (it computed `rows=0` correctly — proven three ways — but React couldn't unmount the stale children, so the unfiltered rows survived the DOM), and **F8d** entries rendered duplicated and out of order (duplicated children landing out of position). ONE bug, two faces. Fix: source-namespaced keys (`r:<seq>` ring / `d:<identity>` disk) anchored on the run's NEWEST entry — so a prepend merging into a run's front only grows `count`, the anchor and key hold, no remount — plus an in-pass `Set` uniqueness guard (a defensive net for the astronomically-rare two-identical-disk-payloads case, never the routine path); and `entryKey` extended to full-payload identity (seq excluded so twins still match) to stop distinct same-ms events being merged (the old ts+logger+message+ipo_id key over-deduped — silent loss). +3 regression fences, EACH shown to fail on the old code: key-uniqueness on a mixed ring+disk buffer (the disk-index==ring-seq collision), key-stability across a merging prepend, and `prependOlder` keeping distinct same-ms events. DIAGNOSIS NOTE worth recording: three headless environments (dev StrictMode, dev single-render, a production `vite build`) all filtered correctly — the cause was localised only when the operator ran DEV MODE on their own machine and captured React's own duplicate-key warning (`Encountered two children with the same key, 6-records_from_vm`), the `<index>-<message>` shape that names the exact bug; collisions require both ring (seq) and disk (index) rows in one buffer, which only happens after scroll-up. Live-proven in the exact field-failure scenario (a 433-row ring+disk mix): `warn`→8 warns, `error`→5 errs, `all`→433 chronological, zero duplicated rows, no duplicate-key warning. PWA-only. Off the scoring path (guard OK, MAX \|Δprob\|=0.0). |
| 2026-07-22 | F1-rev — "Start minimized" defeated on auto-launch by the maximize leak | bug (shell) | **merged `e219154`** (fix `52af562`); **code-proven headless; `.exe` reboot-checklist (a)–(e) LIVE-PROOF PENDING** | `main.ts:174` called `win.maximize()` on the `show:false` window BEFORE the `ready-to-show` start-minimized decision; per the Electron docs `maximize()` "will also show (but not focus) the window if it isn't being displayed already" (confirmed by an isolated E41.10.2 probe — maximize()-on-hidden shows, minimize()-on-hidden does not, disproving the initial guess), so a window last closed MAXIMIZED came up fully shown (tray-ON: start-minimized broken) or flashed maximized then minimized to the taskbar (tray-OFF). One root cause, two symptoms gated on `saved.maximized`, which the operator isolated via a controlled two-reboot experiment (same config, only the maximized state differing). Fix (structural, not one-line): a single pure `startupWindowState({autoLaunched, startMinimized, minimizeToTray, savedMaximized, trayAvailable})` authority in `settings.ts` → `hidden-to-tray · minimized-to-taskbar · shown-maximized · shown-normal`, applied atomically in ONE `ready-to-show` switch (the `:174` maximize deleted); maximize folded into the SHOWN outcomes only; a `trayAvailable` input so a tray-creation failure degrades `hidden-to-tray`→`minimized-to-taskbar` rather than hiding into nothing. Shell only; MAX \|Δprob\|=0.0. Gate: desktop tsc + 26 node --test (6 new: all four outcomes + both leak-closed savedMaximized pairs + the OP-1 invariant + the tray-failure degrade). Deferred do-NOT-fix follow-up logged: `persistBounds` skips `maximize`/`unmaximize` (they don't fire `resized` — E41.10.2 probe), a narrow no-close-second-instance residual. Off the scoring path (guard OK). |
| 2026-07-22 | F10 — Allotment cards retire 5 days after listing (was 7) | bug (display) | **merged `576fda7`** (fix `5f28325`); **code-proven headless; live-proof pending (the Allotment count drops on the next build)** | The Allotment tab kept listed IPOs' cards visible for 7 days after listing; the operator wanted 5. NOT a missing feature — the retirement logic already existed and worked; the window was simply longer than wanted. Fix: `_LISTED_VISIBLE_DAYS` 7 → 5 in `service/ipo_context.py` (its sole consumer), with boundary tests pinning BOTH sides (exactly 5 days after listing → still shown; 6 → dropped). Pure display filter — no data deleted, off the scoring path. Deferred do-NOT-build follow-up logged (config-lift: move `_LISTED_VISIBLE_DAYS` + `_CACHE_STALE_DAYS` into `config/default.yaml` — hygiene, not a bug). Gate: pytest 603; guard OK, MAX \|Δprob\|=0.0. |
| 2026-07-22 | F5 — Broker Cost Assumptions inputs rejected decimals | bug (input) | **merged `c237691`** (fix `849973f`); **code-proven headless + dev-preview-confirmed; not yet `.exe`-verified** | A controlled cost input parsed on every keystroke: typing `0.` → `parseFloat("0.")` → `0` → React rewrote the DOM value → the decimal was destroyed at keystroke 2, so `0.05` landed as `5`. Fix: a draft-string while typing, committed on blur/Enter via a pure `commitCost(raw, fallback, max)` seam — `Number()` over `parseFloat` (rejects `"5abc"` instead of silently taking `5`), one unified rule (valid → commit, invalid/empty/negative → REVERT to the last good value, nothing silently substituted; the old `\|\| 0` meant clearing a field committed a wrong cost of ZERO), `inputMode="decimal"` + an unmount flush. Incidentally removed a per-keystroke `setCosts` + `invalidateQueries(['history'])` storm (now one commit on blur). Off the scoring path — the costs feed only the History net-of-cost DISPLAY, never a probability/verdict. Proof: node --test on `commitCost` (the draft survives `0`→`0.`→`0.0`→`0.05`; invalid reverts; the zero-footgun closed) + a dev-preview confirmation. Gate: pytest 601 · PWA tsc + 99 node --test + vite build. Guard OK, MAX \|Δprob\|=0.0. |
| 2026-07-22 | OP-2 Phase 2 — chip shows `Checked HH:MM` from the new `IngestState.last_pull_ok` → `StatusView.checked_at`; refresh acknowledgments (`Up to date ✓` / `New data ✓` / `Couldn't refresh`, none on a debounced press) + a static delay-disclosure tooltip | bug + feature (freshness UX) | **merged `cc0ec0f`** (feature `c2ffa02`, footgun fence `b2162fe`, dev harness `a4013aa`); **code-proven, headless — no `.exe`** | Grounding found NO honest "app's-last-successful-pull" clock existed: `last_successful_ingest` is the served data's timestamp (the VM's `refreshed_at`), `last_attempt` is data-ts-on-success / wall-clock-on-failure (inconsistent), and client `dataUpdatedAt` advances even on a failed VM pull — and a successful "nothing newer" VM re-pull moves ZERO observable `/status` fields, so no client-only fix could be honest (this made it a backend change, not display-only). New `IngestState.last_pull_ok` (wall-clock) → `StatusView.checked_at` → TS mirror; set in `record_success` (new `pulled_at`, defaults to `when` for the local scrape where scrape time IS pull time; the VM path passes `clock()`) and `record_no_freshness`, NEVER `record_failure` (a failed pull must not overwrite the honest last-check time). Chip primary line = `Checked HH:MM` (the app pull, not the VM stamp); tooltip = STATIC "Quotes data may be delayed up to 30 minutes" — `refreshed_at` deliberately NOT surfaced (stays in the V3-16 console). `refreshAck(before, after)` is pure + node-tested; NO ack on a debounced/coalesced press (never claim a phantom check); precedence isError > refreshInFlight > ack > steady. No-shift proven by real rendered pixels (the five 16ch-floor labels all 144px; degraded suffixes ~209px, unchanged). Held: degraded suffixes, review #6 (checked_at DOES advance on `record_no_freshness` while `last_success` stays null — operator-endorsed), BUG-1. Footgun fence `b2162fe`: a deterministic test pins the VM-path pull clock ≠ the VM's `refreshed_at`, failing loudly if `pulled_at=clock()` is dropped. Dev-only sync-chip gallery `pwa/dev/chip-gallery.{html,ts}` kept — driven by the shipped `syncChip()` so it can't drift — and PROVEN not shipped (clean `dist/` = index.html + assets only; `dev/` isn't publicDir). Gate: mypy 0 · pytest 601 · PWA tsc + 94 node --test + vite build · desktop tsc + 20. Off the scoring path (guard OK, MAX \|Δprob\|=0.0). |
| 2026-07-21 | .exe verification pass — the two-eyes GUI/OS run on a real packaged Electron 41 `.exe` (build `622f68f`); all 8 items passed | verification (`.exe`) | **completed 2026-07-21 on build `622f68f`** (a verification milestone, no merge); **8/8 passed** | The manual two-eyes pass on the packaged E41 installer. LIVE-PROVED the code-proven-but-live-pending items: **L4** (E41 packs, installs, boots), **OP-1** (auto-launch minimizes / a manual open shows), **OP-5** (single-instance: a second launch restores the one window, ONE engine in Task Manager), **review #5** (a real foreign navigation is blocked), **OP-6** (Ctrl+Shift+I / F12 inert; the V3-16 backtick console still works), **OP-3** (full quit+reopen → no re-notify, the unread badge stays clear). CAPTURED the OP-2 console reading (`records_from_vm count=7` · `context_from_vm ipos=400` · `scheduler_cycle_done scored=10` · `stdin_refresh_outcome source=vm · advanced=false · attempted=false`) that specified Phase 2 — the button works; a successful "nothing newer" pull resolved silently, which read as dead. Surfaced **7 new findings** logged as follow-ups (F1-rev, F5, F7, F8, F10, F11, F12); **4 CC-originated findings DISSOLVED** under clean single-install conditions (F1-orig, F2, F3, F6 — artifacts of out-of-band `settings.json` shell edits during the pass, not real bugs; `saveSettings` proven sound on a clean-slate re-test). A verification event — no code change. |
| 2026-07-20 | EB26 sign migration — `win.sign` → `win.signtoolOptions.sign` (electron-builder 26 removed the top-level `win.sign` key) | chore (packaging) | **merged `622f68f`** (fix `13e7eec`); **proven by the build itself** — the E41 installer packs and boots | electron-builder 26 (pulled in by L4's E33→E41 bump) removed the top-level `win.sign` config key, so the E41 packaged build broke at pack time. Migrated the signing config to `win.signtoolOptions.sign`. No verification harness needed — the fix is proven by the build itself: the E41 `.exe` installer now packs and the app boots, and the 2026-07-21 two-eyes `.exe` pass ran on exactly this build. Packaging config only — no app logic, no scoring path. |
| 2026-07-20 | saveSettings atomic — the last non-atomic durable write made atomic (temp + rename), mirroring `saveSeenState` | bug (durability) | **merged `84ccdbd`** (fix `6858662`); **code-proven headless** | `saveSettings` (the shell's durable prefs write) was the last remaining non-atomic durable write in the codebase — a plain write a crash mid-flight could leave torn. Made atomic (write-temp + rename), mirroring `saveSeenState` (OP-3) and the engine-side atomic stores (durability #2/#3/#7, `IngestStateStore`). **Milestone: every durable write in the codebase — engine AND shell — is now atomic (crash-safe).** Shell only; off the scoring path (guard OK). |
| 2026-07-20 | OP-3 — the bell re-notified already-seen items after a restart; a durable `seen-state.json` store (atomic write, new `seen:get`/`seen:set` IPC), kept out of the high-churn `settings.json` | bug (shell) | **merged `6aa8486`** (fix `c66d7e9`); **merged + LIVE-PROVEN** (`.exe`: full quit+reopen → no re-notify, the unread badge stays clear) | The alert bell's seen-set did not durably survive a desktop restart, so a full quit+reopen re-notified items the user had already seen. Fix: a separate durable `seen-state.json` store written atomically (temp + rename), with new `seen:get`/`seen:set` IPC, deliberately kept OUT of the high-churn `settings.json` so a settings write can't clobber seen-state (and vice-versa). Live-proven in the 2026-07-21 `.exe` pass. NOTE: review #8's CHANGED-badge persistence rides on this store and remains UNTESTED for lack of data — no verdict has moved yet, so there is nothing to persist-and-survive. Shell only; off the scoring path (guard OK). |
| 2026-07-20 | OP-6 — Chromium DevTools left on in the packaged build; `webPreferences.devTools = DEV` via an extracted pure `buildWebPreferences()` + a posture-lock test | security (shell) | **merged `f0bc609`** (fix `104a4bc`); **merged + LIVE-PROVEN** (`.exe`: Ctrl+Shift+I and F12 inert; the V3-16 backtick console still works) | The packaged build shipped with Chromium DevTools reachable (Ctrl+Shift+I / F12), an open inspection surface on a sealed shell. Fix: `webPreferences.devTools = DEV` (on only in development) via an extracted pure `buildWebPreferences()`, plus a posture-lock test that fences `devTools`/`contextIsolation`/`nodeIntegration` so the hardened posture can't silently regress. Live-proven in the 2026-07-21 `.exe` pass: DevTools shortcuts inert while the V3-16 in-app backtick debug console still works (the intended inspection path is unaffected). Shell only; off the scoring path (guard OK). |
| 2026-07-20 | L5 — Telegram-bot-token redaction: add a Telegram shape to `_SECRET_PATTERNS` (core/logging.py) | security (hygiene) | **merged `7b2346c`** (fix `2654a1d`); **code + headless-proven** | The pattern layer caught JWT/Bearer/PAN but had no Telegram-bot-token shape, so a token under an innocuous key (`url`/`detail`) bypassed the key denylist and logged in the clear — a real gap since the app builds `/bot<token>/` Bot-API URLs (`service/telegram.py`) and URLs get logged routinely. Added `(?<!\d)\d{5,}:[A-Za-z0-9_-]{34,45}(?![A-Za-z0-9_-])`: `(?<!\d)` lead (NOT `\b`) catches the token glued to `bot` in the URL (a word boundary fails at `t`→digit — the primary exposure); `{34,45}` secret length is EMPIRICAL (Telegram docs example 34, wild ~35 — noted in-code, widen if lengthened); the 34-char minimum is the false-positive guard (spares the ubiquitous `127.0.0.1:<port>`, ISO timestamps, `key:value`); no A-prefix (observed-not-specced → requiring it would risk a false-negative = a leaked token). Surgical `sub()` → `/bot[REDACTED]/` (URL context preserved). Defense-in-depth (the key denylist still catches the well-keyed case). Off the scoring path — `core/logging.py` is the guard's allowlisted write-only sink (MAX \|Δprob\|=0.0). +2 tests: surgical-redact (raw token gone, host+path survive) + FP-guard (innocent colon text byte-identical). CI pytest 595, both jobs green. |
| 2026-07-20 | L4 — Electron 33 (EOL) → 41 upgrade (+ electron-builder 26, @types/node 24) | security + deps (shell) | **merged `65763fb`** (fix `1fb8e83`); **code + dev-smoke-tested + CI-green + LIVE-PROVEN** (two-eyes `.exe` pass, 2026-07-21) | EOL Electron 33 (~8 majors behind, unpatched Chromium, plausibly Store-non-compliant) → **41.10.2** (Chromium 146, Node 24); electron-builder 25→**26.15.3**; @types/node 22→**24**. `npm install`: **0 vulnerabilities**. Breaking-changes audit across all 8 majors (33→41) — authoritative Electron breaking-changes doc (covers 12→44) + E40/E41 release notes fetched (they postdate the training cutoff), cross-referenced against the real API inventory (main.ts/preload.ts/sidecar.ts/settings.ts) — found **ZERO** changes to any shell API we use (setWindowOpenHandler/will-navigate #5, setLoginItemSettings+args OP-1, requestSingleInstanceLock/second-instance OP-5, webPreferences, contextBridge-wrappers, Tray, shell.openExternal); sidecar uses only stable Node built-ins + CommonJS, unaffected by Node 20→24. So **NO code changes** — package.json + package-lock.json only. Headless gate green (desktop tsc + 15 node --test, sidecar /health GREEN, Python mypy 0 / pytest). CC dev-smoke-test on E41: window booted, engine spawned on Node 24, /health green (log+curl), the renderer fired /board+/transitions+/status+/calibration+/allotment (React mounted on Chromium 146), `[nav] blocked` absent, zero boot errors. CI both jobs green with the new lockfile (Linux `npm ci`). **Live-proven (the 2026-07-21 two-eyes `.exe` pass):** electron-builder 26 packs a working `.exe`/MSIX and the app boots on E41, and the real GUI/OS behaviors were confirmed — this pass doubled as the OP-1/OP-5/#5 (and OP-6/OP-3) `.exe` verification on E41; residual closed. Off the scoring path (MAX \|Δprob\|=0.0). |
| 2026-07-20 | L3 — Allotment.tsx date off-by-one (`new Date("YYYY-MM-DD")` parses midnight-UTC) | bug (display) | **merged `5d06403`** (fix `c904317`); **code + headless-proven** | `new Date("YYYY-MM-DD")` parses as midnight UTC then renders in the browser's local zone, so a browser behind UTC (the Americas) showed the PREVIOUS day; latent for IST users (UTC+5:30 lands same-day). Diagnosis traced INPUTS not names: the ONLY buggy site was `Allotment.fmtDate` (close_date/listing_date, date-only); Detail/AlertCenter parse `t.asof` which is a `datetime` (unambiguous — NOT the bug), correctly left alone, as were all datetime formatters, `midnight()` arithmetic, and History's raw-string render. Fixed via one shared home `pwa/src/dates.ts` (`parseDateOnly` = `new Date(iso+'T00:00:00')` local-midnight + `formatDateOnly`); Allotment fixed + Live's correct-but-duplicated `fmtOpen` folded in (anti-drift, proven byte-identical). Display-only (registrar/lifecycle fields; off the scoring path, MAX \|Δprob\|=0.0). Proof: a tz-independent invariant + a byte-identical migration test + a child `node` process with `TZ=America/Los_Angeles` importing the real helper (day 16 vs naive 15), which gates on the UTC CI runner. Separate future item (NOT L3): datetime formatters render in browser-local tz not IST — a Ground-Rule-8 concern logged distinctly. |
| 2026-07-20 | L2 — fetch_vix.py naive `fromtimestamp` filed VIX closes under the machine-local day | bug (data hygiene) | **merged `d893912`** (fix `a431e68`); **code + headless-proven** | `scripts/fetch_vix.py` used `datetime.fromtimestamp(stamp)` (no tz), so a Yahoo daily-bar epoch was read in the machine's local zone — on a machine west of IST (the Americas) a VIX close was filed a day early, shifting which IPOs get the weight-0 cold-market flag (`VixSeries` keys the CSV date against IST IPO close-dates). Bounded: flag-only, never a probability/verdict. Bites machines WEST of IST, not the UTC CI runner (the VIX session is 03:45–10:00 UTC = same calendar day). **Forward-correctness only — the existing `vix.csv` is already clean (0 weekend dates); no data repair** (the append-only merge never mutates stored dates). Fixed via `_ist_date` (`fromtimestamp(stamp, tz=IST)`, existing `ipo.core.constants.IST`, fixed +5:30/no-DST) + IST-anchored the `p1`/`p2` window bounds (hygiene, proven data-neutral: contiguous windows + merge-by-date). Off the scoring path (byte-equality guard `test_vix_flag_wiring.py` stays green; MAX \|Δprob\|=0.0). Proof: a tz-munge (`TZ=America/Los_Angeles` + `time.tzset()`, Unix-guarded) that ran on the Linux CI runner. |
| 2026-07-20 | L1 — gmp.py silent row drops (dropped as obsolete; do not re-propose) | bug (dropped) | **won't-do — dropped as obsolete** | GMP is being removed from the repo in a future session, so instrumenting/logging a dying code path was not worth doing. Recorded here (per the V3-7/V3-10 dropped-row precedent) so it carries its do-not-re-propose reasoning and cannot be silently re-surfaced later. No code change. |
| 2026-07-20 | Review #11 — code-review finding: CI ran neither package's TypeScript tests (both TS suites unenforced) | infra (CI) | **merged `dd7aaaa`** (fix `47301e4`); **code + live-proven** (CI green on `origin/main`) | `ci.yml` ran Python only (ruff/black/mypy/pytest), so every TS unit test was unenforced — incl. the desktop fail-closed `isAllowedExternalUrl`/`isAllowedRhpUrl` phishing allowlist and the PWA `isAllowedNavigation` guard; the header's "this workflow is the enforcement" was true for Python only. Added a separate `frontend` job (parallel to `quality`, independent red/green) on ubuntu-latest / **Node 24** (REQUIRED — the PWA runs `.ts` directly, needing default type-stripping ≥22.18/≥24; Node 20 would hard-fail): PWA `npm ci` + `typecheck` + `npm test`; desktop `npm ci` (`ELECTRON_SKIP_BINARY_DOWNLOAD=1` — tsc needs only Electron's types, the tests are pure logic that never launch Electron) + `npm test` (tsc whole-package build + node --test). **Load-bearing fix folded in:** the PWA `test` script's UNQUOTED `src/**/*.test.ts` silently under-ran under Linux `/bin/sh` (no globstar) → 3 of 7 files, dropping the 4 top-level ones incl. `external.test.ts` (the phishing test #11 exists to protect) — wiring it in as-is would have been a false-green on the exact security test. Double-quoting (`"src/**/*.test.ts"`) delegates the recursive glob to Node's runner, consistent on cmd + sh (proven: unquoted-sh=3, quoted-sh=75/7). Live-proven on `origin/main` (run 29736700307): **both jobs green — quality (mypy 0 + pytest 590) AND frontend (PWA 75/75 + desktop 15/15 = 90 TS tests on Linux)**, counts read from the CI logs. Off the scoring path (guard OK, MAX \|Δprob\|=0.0). |
| 2026-07-20 | Baseline retirement — narrow `_run_cycle_guarded` to a `_GuardableService` Protocol so mypy exits 0 | infra (CI/typing) | **merged `0779644`** (fix `797b107`); **code + live-proven** (CI `quality` green on `origin/main`) | The #11 CI run revealed `quality` had been RED on every recent `main` commit: bare `mypy` (all 182 files incl. tests) hit 4 `arg-type` errors in `tests/unit/test_runner_paths.py` and exited 1, so **pytest — which runs after — had NEVER executed on CI**. Root cause was over-declaration, not dishonest doubles: `_run_cycle_guarded` touches only two members of its argument (`run_cycle()`; `scheduler.next_cadence_minutes()`) but was typed as the full 5-field composed `Service`; the B1/B2 doubles `_OkService`/`_BoomService` honestly provide exactly those two yet aren't nominal `Service` instances. Fix (interface segregation): a narrow `_GuardableService` Protocol capturing just that surface — `scheduler` a read-only `@property` (covariant, so a `ScoringScheduler` subtype is accepted) and `run_cycle -> object` (covariant return, so a tuple-returning real method and a `None`-returning double both match). Both the real `Service` and the doubles satisfy it structurally; mypy exits 0 for the RIGHT reason. **Test file untouched, zero `# type: ignore`.** **Retires the "4 parked mypy errors / zero-new-vs-baseline" convention → the gate is now zero mypy errors, full stop.** This is what let `quality` reach green and pytest actually run on CI for the first time (silently skipped on every prior red commit). Off the scoring path (guard OK, MAX \|Δprob\|=0.0). |
| 2026-07-20 | Review #10 — code-review finding: CORS `*` on the local engine sidecar | security (won't-fix) | **merged `1a9614f`** (commit `44b7a82`); **reviewed → WON'T-FIX (intentional, documented)** | Considered accept, not an oversight: the engine is a LOCAL sidecar bound to 127.0.0.1 (not network-reachable), GET-only (Invariant 4 — cannot mutate), and credential-less (no cookies/auth → no CSRF/session surface); the packaged app loads from `file://`, whose Origin serializes to the literal "null", so there is no clean origin to allowlist and `*` is the honest value. If results are ever paywalled the plan is to move the engine SERVER-SIDE behind real auth (cf. `vm/server.py`) — CORS was never a sufficient paywall boundary, so it is not the lever to tighten here. Behavior unchanged (`allow_origins=["*"]` + `allow_methods=["GET"]` kept, so the packaged app reaches its engine exactly as before); `allow_headers` narrowed `["*"] → ["accept"]` (the sole header `client.ts` sends) — provably inert since a GET carrying only `accept` is a CORS simple request (no preflight → `allow_headers` never consulted). The rationale is now documented in `api.py`'s comment. `vm/server.py`'s identical `["*"]` — a PUBLIC, network-reachable server, a different risk profile — is flagged as a separate future consideration, NOT touched. No logic change → no new test (full suite incl. `test_api.py` stayed green). Off the scoring path (guard OK, MAX \|Δprob\|=0.0). |
| 2026-07-20 | Review #9 — code-review finding: no React error boundary (one bad field white-screened the whole app) | bug (code-review) | **merged `ed7c86f`** (fix `874b639`); **code + headless-proven** | `main.tsx` mounted the app with no error boundary, so any uncaught render throw (a malformed field, an unexpected null, a bad engine shape) propagated to the root and React 18 unmounted the WHOLE tree — a blank white screen, no recovery. Fix: two boundaries — a CONTENT boundary around the routed content region, keyed on the route (`view`+`detailId`) via `contentBoundaryKey` so navigating away from a crashed screen AUTO-resets it while the sidebar/topbar survive (a broken Detail never traps the user); plus a thin ROOT backstop around `<App/>` for a throw in the shell itself (reload-only). Both reuse the `.enginedown` visual language; `error.message` shows as one muted line, the component stack goes to `console.error` only (renderer process — NOT the V3-16 engine `/logs`, the #5 distinction). The ONE class component in the hooks codebase (React exposes the catch lifecycle only on classes), headed with that reason; substantive logic in the pure JSX-free `boundary.ts` seam (named `boundary.ts` NOT `errorboundary.ts` to dodge a case-collision with `ErrorBoundary.tsx` — the exact class of bug Linux CI now catches). **Honest proof:** the pure decision logic (`deriveErrorState`/`contentBoundaryKey`/`formatBoundaryLog`) is `node --test`'d; "React actually invokes `getDerivedStateFromError` on a child throw" is React's own guarantee — a probe confirmed React 18 `renderToStaticMarkup` re-throws rather than invoking the boundary, so it can't be driven headlessly and NO jsdom/RTL E2E was faked. PWA only; off the scoring path (guard OK, MAX \|Δprob\|=0.0). |
| 2026-07-20 | Review #8 — code-review finding: CHANGED-badge per-IPO seed-missing-silently + advance-on-Detail-open + reactive read | bug (code-review) | **merged `6385509`** (commit `9a9d2d1`); **code + headless-proven** | The Live "CHANGED" badge (`lastSeen`, a `Record<ipo_id, VerdictType>` in `prefs.ts`) was write-once-seeded — `seedLastSeen` wrote only when `lastSeen` was empty — a frozen first-launch snapshot that never advanced: a lit badge never cleared, and IPOs appearing later never worked. DRIFT flagged (grounded against real code, not the report): the review described "post-launch IPO → `undefined !== verdict` → PERMANENT badge", but current main already had a `!!lastSeen[id] &&` guard, so a post-launch IPO showed **silently NO badge** — the symptom is INVERTED from the review, not applied verbatim. Also a scope bug: the store is app-wide but the baseline was maintained only from Live, so a leave-and-return could false-light. Fix: the load-bearing rule `hasChanged = id in lastSeen && lastSeen[id] !== verdict` (MISSING baseline = NOT changed); per-IPO baselines seeded silently on first LIVE appearance (scoped to the Live rows, never the whole board, so an upcoming/closed verdict can't seed a baseline that later false-lights); `markSeen` advances one baseline on Detail MOUNT so ANY of the five open paths clears it (not just a Live-row click); `useLastSeen` = `useSyncExternalStore(subscribe, getLastSeen)` (the BUG-3 pattern) so the badge drops reactively. Pure seam (`seedMissingBaselines`/`hasChanged`/`withSeen`) node --test'd — 7 cases incl. leaves-and-returns no-false-light, not-frozen-to-install, ref-stability (same map on a no-op, so useSyncExternalStore never loops). Verdict-CATEGORY basis unchanged (never probability). Storage stays `saveLocal()` (localStorage only); `lastSeen` left unpruned (bounded, tiny), OP-3 upgrades all seen-sets' durability later and #8 rides free. PWA only; off the scoring path (guard OK, MAX \|Δprob\|=0.0). |
| 2026-07-20 | Review #5 — code-review finding: navigation lockdown (setWindowOpenHandler deny-all + will-navigate/will-redirect allow-own-origin only) | security (shell) | **merged `fb14c32`** (commit `86b953b`); **code + LIVE-PROVEN** (the 2026-07-21 `.exe` pass) | The `BrowserWindow` had no `will-navigate` / `setWindowOpenHandler` guards, so nothing structurally stopped in-app content (a link, a redirect, injected/compromised content) from navigating the trusted frame away or spawning a popup. Defense-in-depth: `setWindowOpenHandler(() => ({ action: 'deny' }))` denies all popups — safe, because approved external links go out via `shell.openExternal` over IPC (never `window.open`), the preload always exposes `openExternal` so the desktop shell never hits the browser fallback, and there are no `target=_blank` anchors; `will-navigate` + `will-redirect` allow only the app's own origin via the electron-free, node --test'd `isAllowedNavigation` (dev: the dev-server origin; prod: the loaded PWA file's pathname — file:// origins are all "null"), else `preventDefault()` + `console.warn('[nav] blocked', url)` (the shell process's own sink, deliberately NOT the V3-16 engine `/logs`, a different process). Grounded that it trips zero times in normal use: the splash + PWA loads are programmatic (loadFile/loadURL, which don't fire will-navigate) and the PWA has no router. OP-1/OP-5 don't touch `webContents`; `openExternal` is a separate path, untouched. Shell only; off the scoring path (guard OK). Live-proven in the 2026-07-21 `.exe` pass — a real foreign nav is actually blocked — alongside OP-1/OP-5. |
| 2026-07-20 | Review #6 — code-review finding: freshness-honesty (a null/empty VM records envelope no longer recorded as success-at-now) | bug (code-review) | **merged `4f003f7`** (commit `39776ed`); **code + live-proven** | In `data_plane._refresh_records`, a VM records envelope with `refreshed_at=None` was recorded as `record_success(envelope.refreshed_at or clock(), source="vm")` — the `or clock()` stamped NOW, so stale/absent data showed as freshly refreshed (BUG-1 family). **Completes durability #2 — read the two as ONE freshness-honesty fix:** #2 made the VM's /records route null `refreshed_at` server-side on a corrupt/never-ingested store (honest), but this client line then re-stamped "now" and silently UNDID it (#2 = server-honest, #6 = client-honest). `refreshed_at` is None in two cases (VM never ingested; #2's `records_degraded` degrade), both handled the same. Fix (operator decision — the NEUTRAL recording, not `record_failure`, which would falsely show "retrying" for a reachable-but-empty VM): a new `IngestState.record_no_freshness` — `last_attempt=now`, `source="vm"`, `last_error` cleared, `last_attempt_ok=True` (reachable, not a failure), and **`last_success` UNTOUCHED** (None → "awaiting first update"; a prior → last-known). The local-scrape path is deliberately untouched — an empty local scrape is a genuine "confirmed nothing right now", so success-at-now there is correct. Confirmed the path byte-identical since the review (`git diff 41faf33..HEAD` empty). Tests: a null envelope does NOT advance `last_success` to now (None stays None; a seeded prior stays the prior); `syncChip` on that state reads "awaiting"/last-known with no "retrying"/"refreshed". Off the scoring path (guard OK, MAX \|Δprob\|=0.0). |
| 2026-07-20 | OP-2 Phase 1 — freshness-chip flicker fix + auto-pull silence + 600ms manual beat + fixed width + permanent manual-refresh logging | bug + logging | **merged `952d006`** (commit `6e1e376`); chip **code-proven**; button **diagnosis complete** (the 2026-07-21 `.exe` console reading) → resolved by **OP-2 Phase 2** (`cc0ec0f`) | The top-bar SyncStatus chip flipped "Refreshing…" every ~5s: it read `useIsFetching()` (the GLOBAL react-query counter), which pulses on every 5s background re-poll of `/health`+`/status` (`refetchInterval: 5000`) — LOCAL sidecar reads, not NSE pulls (`StatusView` has no in-progress field), so the client cannot know an automatic pull is in flight. Fix: "Refreshing…" binds to a genuine manual pull (a small `RefreshContext`, one source of truth for RefreshButton + SyncStatus) with a 600ms min-visible beat (`REFRESH_MIN_VISIBLE_MS`); auto-pulls silent; fixed 16ch chip width so the ok↔busy flip never resizes. The chip-state decision is the pure `syncChip` in `status.ts`, tested — incl. THE FLICKER regression (background poll → "Updated", NOT "Refreshing…"). Plus PERMANENT engine instrumentation (`stdin_refresh_requested` on receipt + `stdin_refresh_outcome` via pure `classify_refresh_outcome`): a manual refresh is a VM RE-PULL (`data_plane._refresh_records`), and `record_success` sets `last_success` to the VM's own `refreshed_at`, so an unchanged clock is CORRECT when the VM has nothing newer — the outcome carries `source` (vm/local) + `refreshed_at` so the button-cases stay distinct (VM-nothing-newer / display-bug / local-scrape / fetch-failure), the split the old logging couldn't make (`refresh_from_nse` never raises). Redaction-at-sink verified for the new lines. PWA + engine-logging only; off the scoring path (guard OK, MAX \|Δprob\|=0.0). Did NOT touch button behavior — Phase 2 gated on the operator's `.exe`/dev console reading, which the 2026-07-21 `.exe` pass captured (`stdin_refresh_outcome source=vm · advanced=false`) and OP-2 Phase 2 (`cc0ec0f`) then shipped. |
| 2026-07-20 | OP-5 — single-instance lock; a second launch restores the existing window (no second engine) | bug | **merged `53d9d88`** (commit `f17b8c4`); **code + LIVE-PROVEN** (the 2026-07-21 `.exe` pass, validated as a pair with OP-1) | When minimized to tray, launching the app again spawned a SECOND instance — a second window AND a second Python engine writing the same `engine-data` dir (incl. `verdict_transitions.json`), a two-writer race the durability cluster's single-writer atomic writes structurally can't close. Fix: `app.requestSingleInstanceLock()` acquired at the entry point BEFORE `whenReady` fires `boot()` (where the engine spawns), so a second launch quits before starting a second engine or window; the `second-instance` handler restores the running window via the existing `showWindow()` (the one restore home the tray uses) and IGNORES the forwarded argv, so a forwarded OP-1 `--ipoadvisor-autostart` marker can't drive minimize. Lock unconditional (dev+prod). NO unit test added — pure Electron orchestration with no non-vacuous pure seam (recorded as a reasoned decision, not a gap). Shell only; off the scoring path (guard OK). Live-proven in the 2026-07-21 `.exe` pass — second launch restores + focuses, ONE `ipo-engine.exe` in Task Manager, no second window — validated as a pair with OP-1 (one rebuild covered both). |
| 2026-07-20 | OP-1 — "Start minimized" applies only on auto-launch, not a manual open; one-time login-item marker migration | bug | **merged `4d95486`** (commit `dc8fa02`); **code + LIVE-PROVEN** (the 2026-07-21 `.exe` pass, validated as a pair with OP-5) | `startMinimized` took effect on EVERY launch — a manual double-click/Start/taskbar open was hidden too. Windows can't use `getLoginItemSettings().wasOpenedAtLogin` (macOS-only), so the robust signal is a marker arg on the login item: register `args: ['--ipoadvisor-autostart']`, and apply start-minimized only when `wasAutoLaunched(process.argv)`; a manual open always shows. Existing "launch on startup" users have a marker-less login item, so a ONE-TIME flag-guarded migration (`planStartupMigration` + `startupMigrated`) re-registers it with the marker exactly once, then NEVER re-asserts on later boots — so a user who disables auto-launch via Windows Task Manager has that stick (the app must not override an OS choice every boot). Pure logic in electron-free `settings.ts` (tested: wasAutoLaunched, loginItemSettings, migrate-once-then-no-op); `main.ts` a thin caller. Settings sub-label corrected to scope the claim to startup. Shell only; off the scoring path (guard OK). Live-proven in the 2026-07-21 `.exe` pass (auto-launch minimizes / a manual open shows), validated as a pair with OP-5. |
| 2026-07-20 | Durability #7 — code-review finding: base.py RawCache atomic write + poisoned-read SourceError wrap + quarantine/self-heal | bug (code-review) | **merged `90eaf58`** (commit `fe6571c`); **live-proven** (tests + live demo) | `RawCache` wrote entries non-atomically and read them unguarded; a poisoned entry raised a ValueError-family exception (NOT `SourceError`), which escaped `pipeline.py`'s per-source fault isolation AND the live path's, aborting the WHOLE ingest run. Empirically confirmed (probe, not reputation): a torn/garbage entry raises `UnicodeDecodeError` at the utf-8 DECODE step (before `json.loads`), all corruption modes ⊂ `ValueError`. Fix: atomic tmp+`os.replace` store (a crash never leaves a torn, permanently-poisoning entry); on a corrupt read, quarantine the poisoned file (`<hash>.json.corrupt`, best-effort) AND raise `SourceError` so it re-enters isolation and the write-once-immutable cache self-heals on re-fetch. Catch `(OSError, ValueError, ValidationError)`. Quarantine-on-read is DELIBERATELY unlike #2 (internal, reproducible ingest cache). Headline test: a poisoned entry degrades one source but the run completes 7/7 on the good source. Off the scoring path (guard OK, MAX \|Δprob\|=0.0); third of the durability cluster. |
| 2026-07-20 | Durability #2 — code-review finding: repository.py atomic Parquet write + degrade-to-empty read + records_degraded→honest refreshed_at | bug (code-review) | **merged `dae1883`** (commit `024d91a`); **live-proven** (tests + live demo) | `repository.py` wrote both Parquet tables non-atomically and read them unguarded in `__init__`; because `vm/server.py` builds a fresh `ParquetRepository` per `/records` request, a torn `ipo_records.parquet` was a PERMANENT per-request 500 outage. Empirically confirmed a garbage/truncated/empty Parquet raises `pyarrow.ArrowInvalid` (⊂ `ValueError`). Fix: atomic tmp+`os.replace` for both writes; read degrades to honest-empty + a loud WARN, NO quarantine (a GET-only VM read stays side-effect-free, matching `/context`); a `records_degraded` flag lets `/records` null a stale `refreshed_at` on corruption while KEEPING it for a genuinely empty-but-fresh ingest (composes with the parked review #6). Degrade kept universal — the local app boot no longer crashes, read-then-write ingest jobs self-heal on the next write. Off the scoring path (guard OK, MAX \|Δprob\|=0.0); second of the durability cluster. |
| 2026-07-20 | Durability #3 — code-review finding: transitions.py atomic write + quarantine/start-empty/honest-WARN on corrupt read | bug (code-review) | **merged `c51ab6b`** (commit `c1be886`); **live-proven** (tests + live demo) | `verdict_transitions.json` was written non-atomically and read unguarded in the constructor, so one interrupted write raised `JSONDecodeError` out of `TransitionStore.__init__` and killed engine boot PERMANENTLY. Fix: atomic tmp+`os.replace` write; on a corrupt read, quarantine the file (`.corrupt`, bytes preserved) + start empty + a loud WARN, NEVER raise; read switched to `utf-8-sig`. The log is NON-REPRODUCIBLE and has NO backup anywhere (the revised V3-2 archive mirrors only records + context — verified against `vm_archive_snapshot.py`), so the WARN says so honestly (pre-corruption history is unrecoverable) and flags the OP-3 re-prime caveat rather than pointing at an archive that doesn't hold this file. First of the three-item durability cluster (the code review was taken at `41faf33`). Off the scoring path (guard OK, MAX \|Δprob\|=0.0). |
| 2026-07-19 | Progress-log reconciliation against `main` — V3-16 status, three orphaned SHAs, the missing 2026-07-18 session | docs (integrity) | **corrected in place**; `test_progress_log_integrity.py` green | Triggered by a git-truth check on V3-16: the log banner, the V3-16 row, and the blueprint all still described it as "built on branch `feat/v3-16-console`, pending operator review + the shipped-`.exe` visual proof" although it **merged as `1693e48` on 2026-07-16** — the doc-update-at-merge step was skipped for that row, so it never received the `merged <sha>` marker every sibling carries, and the branch was pruned afterwards leaving nothing to notice. Running the same audit across **every** row rather than just that one found four further drifts: **Finding-④** and **V3-11** still read "(branch `feat/v3-11-context-fields`, pending review)" though they merged as `7d06c4c` and `ba9bba4`; **three rows cited pre-rebase SHAs** (`8b1d337`, `ce45e82`, `b4b78bd`) that exist only on `dev/pre-exe-visual-qa-fixtures` and are **unreachable from `main`** — the commits that actually landed are `e1fa599`, `f47b4d6`, `920303b`, all under merge `477d220`; and the **entire 2026-07-18 session** (five commits, `b0d74f4`→`41faf33`) was absent from the log. All corrected here; every SHA the table now cites is machine-checked to exist **and** to be an ancestor of `main`. `V3-16` added to the integrity guard's `_SHIPPED` ledger. Process note: the 2026-07-18 commits are the first to land **directly on `main`** (single-parent, no merge commit) rather than branch-first — with no merge step, there was no moment at which the log was conventionally updated. |
| 2026-07-18 | Alert badge re-lit after a restart — the BUG-2 prune erased its own seen-set on a cold start | bug | **merged `41faf33`** (direct to `main`) | Operator-reported: clearing the alert badge did not survive closing and reopening the app. Cause is a **self-inflicted erase, not a load failure** — `AlertCenter` prunes its persisted seen-set down to ids still on the board (the **BUG 2** retention rule, so the set can't grow unboundedly as IPOs list and leave), and that prune ran on mount. On a cold start the board prop is `[]` until React Query resolves, so the prune matched nothing, dropped every id, and **persisted the empty set**; when the board then arrived, every already-cleared alert counted as unread again. The seen-state was loading correctly from `localStorage` and being erased a moment later by the guard meant to bound it. Fix: `pruneSeenIds()` — the same prune, but **never against an empty board**. Skipping is safe because an empty board also cannot *grow* the set: ids only ever enter it from `currentApplyAlerts`, derived from those same rows, so "no rows" means "nothing to add and nothing to judge against" — wait for data rather than erase. Kept as a pure helper in `alerts.ts` (not an inline component guard) so the regression sits under the existing `node --test` pure-logic suite. +2 tests (empty board preserves the set; a loaded board still prunes); 34/34 green, `tsc` clean. Front end only — no engine, no scoring path. |
| 2026-07-18 | Release 0.8.1 — ship the BUG-4 context-cache fix | chore (release) | **merged `b89b359`** (direct to `main`) | 0.8.0 (built 2026-07-17) predates the BUG-4 fix (`7b4de4f`), so **every install from that build — including the Store package — serves `available=false` / `not_loaded`** on the Allotment tab and on every RHP/registrar/ISIN field whenever the engine starts against a data dir with no context cache yet. A fresh install is exactly that case, which is why it was invisible on developer machines. Bumps both `package.json` files and the hardcoded About string together — the About line is literal text in `Settings.tsx`, not derived from either `package.json`, so it would otherwise keep reporting the previous version regardless of what actually shipped (the same trap the 0.8.0 bump hit). No app logic in this commit; the fix itself is `7b4de4f`. |
| 2026-07-18 | Per-IP rate limit on the VM read-API + the data VM's network state written down | feature (infra) | **merged `6405ca5`** (direct to `main`) | The data VM's read-API is now **public** (port 8000 opened to `0.0.0.0/0` in the OCI console) so every shipped `.exe` reaches it instead of most users silently degrading to local NSE scraping; this commit adds the guard rail that opening it required. **Rate limit:** 60 req/min/IP, fixed window, in `src/ipo/vm/server.py` — deliberately **dependency-free and in-memory** (uvicorn runs a single worker here, so a process-local counter is coherent, and `ipo.vm.server` is a PyInstaller hidden import, so a library would ship inside every desktop `.exe` for code the desktop never runs). Registered **before** CORS so a 429 still carries CORS headers. The tracked-IP map is capped and pruned — an unbounded ip-to-state dict is itself a way to exhaust a 1 GB box. Sized for **CGNAT, not for one user**: normal use is ~2 requests per user per ~30-min ingest cycle, and carriers put many subscribers behind one address. **Network state documented for the first time** (`operations/README.md`): all four ingress rules, the instance-level iptables that must agree with them, the rate limit, and the ephemeral public IP. These rules previously existed in **no repo file**, which is how their state came to be misread — including a confident claim that any user could reach the VM when port 8000 was in fact pinned to one ISP block. **SSH (22) restricted** to the operator's ISP block, a documented deviation from `VM2_RUNBOOK`'s /32 (key-only auth confirmed on the box; the ISP demonstrably reassigns within that block, so /32 would lock SSH out on every reshuffle for a marginal gain). Also corrects a prior claim in the project notes: an OCI **ephemeral public IP does survive stop/start** and cannot be converted to reserved while keeping the same address — the real exposure is termination, and the durable fix is a domain in front of the VM rather than a reserved IP. Serving layer only — no scoring path, model, or app logic (guard clean; the API serves the same data to more clients). |
| 2026-07-18 | BUG-4 — context cache never re-read (stale-forever on a long-lived process) | bug (correctness) | **merged `7b4de4f`** (direct to `main`) | `ContextStore` loaded its cache once at construction and never looked at the file again. `runner.main` builds it at boot, **before** the first `refresh_data_plane` cycle writes that file — so a process started against a data dir with no context cache served `available=false` / `not_loaded` on `/allotment` and `/context/{id}` **for the life of the process**, while correct data sat on disk and every refresh rewrote it. Nearly invisible on the desktop (constant restarts; the data dir usually already holds a cache), serious on a long-lived headless process started against an empty data dir, where the Allotment tab and every RHP/registrar field are dead on arrival, silently. Found via a long-running-server test scenario and diagnosed before being written up — the **BUG-1 class** again (a path that fails while looking fine). Fix: **mtime-guarded reload** — the store keeps its snapshot and re-reads only when `(mtime_ns, size)` moves, so steady state is one `stat` per read and zero parses. The reload is **writer-agnostic by design and must stay that way**: it reacts to the file, never to being told who wrote it, which is what makes it cover the engine's own cycle, an operator running `refresh_context.py` against the app's data dir, and a restore from the durable archive alike. "Have the cycle notify the store" was **considered and rejected** — it encodes writer identity, so the day a second writer appears the store goes silently stale again; parse-on-every-read was rejected too (`build_allotment_view` calls `get()` once per record). **Intra-request snapshot:** `ContextStore.snapshot()` returns one frozen view and both builders take it once per request, closing a window where a refresh landing mid-request could return rows from different cache versions with a freshness line matching none of them. Two companions, each closing a hole the reload itself opened: the context write is now **atomic** (tmp + `os.replace`, matching `IngestStateStore._flush` — the old plain `write_text` was harmless only because nothing re-read the file), and a **failed reload keeps the last-good snapshot** (only `__init__` may start empty). Also corrected `vm/models.py`'s docstring, which already claimed the store "re-validates it on read" — inaccurate when written, true as of this fix. Scoring path untouched, **measured not asserted**: 386 IPOs, 0 verdict changes, 0 reason changes, MAX \|Δprob\| = 0.0; guard clean (`service/` and `data/ingest/` are outside the guarded prefixes). +10 tests including `test_unchanged_file_is_not_reparsed`, which pins the mtime guard **by parse count** so it cannot silently degrade into parse-on-every-read. Recorded as BUG-4 in `docs/PROJECT_LOG.md` §6 (a PROJECT_LOG bug, not a v3 blueprint item — hence no `BUG N` blueprint mapping). |
| 2026-07-18 | Website: legal pages, honesty labels, readability pass, redesigned footer, logo home affordance | feature (website) | **merged `b0d74f4`** (direct to `main`) | Marketing-site pass across all seven pages. **The app's honesty discipline extended to the site:** removed the sole NSE data-source reference from the hero trust row ("Live NSE-aware data" → "Live issue data"), matching the app's rule of never naming the source; and labelled every illustrative number as a sample (a SAMPLE pill beside the 72% outlook card, "sample data" in the hero product-preview mockup's title bar) so neither reads as a live prediction. **Three new legal pages** (`privacy.html`, `terms.html`, `contact.html`) built on the existing detail-page template, with `[SUPPORT_EMAIL]` left as a greppable placeholder. **Footer standardized** across all 7 pages — one shared disclaimer sentence linked to `terms.html` plus a Privacy/Terms/Contact nav row — and redesigned as a full-width dark band with a brand+legal block and Product/Legal link columns, replacing the cramped single-row layout. **Readability:** darkened and consolidated the muted body-copy and label colors (both were sitting at or below the WCAG AA contrast floor) and bumped undersized body text to ~15px, without touching the already-fine dark-section colors. **Header logo is now an obvious home link** — an opacity hover plus a back-arrow that reveals only on hover — so sub-pages don't strand the user without a plain "Home" nav word. Static site only — no engine, no app, no scoring path. |
| 2026-07-17 | Refresh spinner outlasted the pull; console flickered on hold; console overlay faded on close | bug (×3) | **merged `4a03bbb`** (3 commits: `e934c8f`, `2dd6bd4`, `a8895bb`) | Three small operator-reported UI defects, one branch. **Refresh spinner:** `RefreshButton` only cleared on `last_successful_ingest` advancing, so a pull that reached NSE and genuinely *failed* (a completed attempt) spun uselessly for the full 20s, and a click landing inside the engine's 15s stdin debounce (silently swallowed, no cycle runs) always burned the timeout with nothing to show. Now clears as soon as `last_successful_ingest` **or** `last_attempt` moves past the click's baseline — either means the engine did something, success or failure; nothing fabricated, both fields already existed (BUG 1). Fallback timeout (the one case neither signal can see — a debounced-away click) shortened 20s→10s. **Console flicker on hold:** holding `` ` `` re-toggled the console every ~30ms because the OS fires repeated `keydown` events the whole time a key is held, and the handler toggled on every one. `e.repeat` is now ignored outright; the open/close decision moved to `keyup`, split by hold duration (< 350ms = toggle, exactly as before; held past it = shows while held, hides on release — a peek gesture). Verified via synthetic keydown/keyup dispatch in a live preview: quick tap toggles both directions; a hold with repeated `repeat:true` keydowns in between stays closed until the threshold, opens exactly once (no flicker), closes cleanly on release. **Console fade on close:** traced to the global `html.theme-anim *` rule (crossfades tokens on a light/dark switch) having enough specificity to leak a background transition onto `.cl-overlay` if a theme toggle and a console close ever land close together. Added an explicit `transition:none !important` (same pattern as the existing reduced-motion override); verified the computed transition stays `none` even with `theme-anim` forced active on the root. PWA-only, scoring path untouched (guard clean, nothing under `model/`/`calibration/`/`features/`/`core/`/`config/`); 32/32 `node --test` green. |
| 2026-07-17 | Live signals never retired a closed IPO — badge mismatch, then the real fix | bug | **merged `1659023`** (branch `fix/live-signals-retire-on-close`, commit `8d4cacc`) | Operator-reported: two book-closed IPOs (SBI Funds, Alpine Texworld) stayed on Live signals alongside an actually-open one, simultaneously duplicated into History's "Awaiting listing outcome." Investigated properly before touching anything (operator's explicit ask): Live.tsx's own filter, written 2026-07-03, never excluded a closed-but-unlisted row by design ("closed and awaiting listing" was meant to stay). But three other pieces of the codebase — the sidebar badge's count logic, and the commit that built the Awaiting-listing History section — were all written 5 days later on the premise that closed IPOs *already* left Live, which was never true; that premise mismatch is exactly what produced the badge/content disagreement (badge showed 1, list showed 3). Operator confirmed a real firsthand memory of watching an IPO disappear on close in an older build, which didn't match either code path (frontend or backend) as currently written — ruled out via exhaustive commit history on `Live.tsx`, `App.tsx`, and the engine's `/board`/`resolve_listings` path (no source ever implemented a close-date exclusion). Manually checked NSE's live `public-past-issues` feed and Upstox's `/v2/ipos/{id}` for the two IPOs in question along the way — NSE's `listingDate` was a literal `"-"` (genuinely not yet published), Upstox already had a scheduled `timeline.listing_date` (operator-confirmed correct) — a real, separate, useful finding (see the two investigation rows below), but not the cause of this bug. Operator's final call: retire Live on `close_date`, matching the codebase-wide (if inconsistently-implemented) assumption. Fix: `Live.tsx`'s filter gained `&& midnight(r.close_date) >= t` — stays through the close day itself (matches the existing "CLOSES TODAY" behavior), retires the day after, at the exact `close_date < today` boundary History's Awaiting-listing filter already used (zero gap, zero double-show, that file untouched). Fixes the sidebar badge mismatch as a side effect — its logic already computed this exact partition. Verified against the real running engine's live `/board` (not a fixture): before = 3 rows (sbifunds, alpinetex, cmll) on Live; after = cmll only; Awaiting-listing unchanged (already held both). Render-layer only; scoring-path guard clean; `MAX\|Δprob\|=0.0` not just by the guard but because the file has no reachability to the scorer. |
| 2026-07-17 | NSE-derived data — scoring-path classification map (investigation only, no code change) | investigation | **report delivered**, no branch | Operator asked, in the course of the Live-signals investigation: a complete inventory of every NSE-derived field, each traced (grep evidence, not assertion) into `model/`/`calibration/`/`features/`/`core/` or ruled out. 🔴 **scoring-path:** `segment` (kill-flag), `price_band_high`/`issue_price` (GMP-% + `net_listing_return`), `close_date` (the `book_closed` as-of anchor), `qib_sub`/`nii_sub`/`retail_sub` (scorer weights). 🟢 **non-scoring:** `ipo_id`, `company`/`name`, `price_band_low`, `open_date`, `nii_small_sub`/`nii_big_sub`/`overall_sub` (display-only, confirmed zero references outside the type definition), `listing_date`. 🟡 **borderline (the trap):** `listing_open`/`listing_close` — proven absent from the live feature vector by `features/leakage.py`'s own mutation test (they're the canonical example it protects against), but both feed `net_return`/`gross_return` on the live History screen and the offline calibration benchmark — never a model *input*, but load-bearing for calibration validity. Purely a reference map for evaluating any future non-scoring data-source swap; nothing in the app changed. |
| 2026-07-17 | Upstox `listing_date`/`listing_price` reliability, checked against 386 historical IPOs (investigation only, extends PROJECT_LOG §4.9) | investigation | **report delivered**, no branch | Follow-on to the map above: is Upstox reliable enough as a supplementary source for the 🟢/🟡 listing fields? A 3-IPO spot-check looked perfect (exact date + price match); the operator pushed for "as many older IPOs as you can, not just 5 or 10," so all 386 rows of `data/backfill/mainboard_ipos.csv` (2017–2026) were checked against Upstox's live `/v2/ipos/{id}`. Result, at scale, is materially weaker than the small sample suggested: **25% not found** in Upstox's listing at all (a real retention-window gap, not the multi-year archive NSE effectively is via the CSV); `listing_date` matched 96.2% (279/290) when found, but **3 of the 11 mismatches were off by 1–3 years**, not days — a naive `symbol.upper()` lookup (the same matching scheme `resolve_listings()` already uses) can silently latch onto a *different* company that once reused the same ticker, a genuine wrong-match failure mode, not just a missing one; `listing_price` matched only 37% (106/290), with **184/290 simply `None`** — every populated value traced to a listing from roughly the last 18 months, so Upstox appears not to carry the field further back, and even where present it differed from the recorded `listing_open` by 1–6% (a different reference point, not a guaranteed match). Conclusion: Upstox remains good for surfacing an early, clearly-labeled *estimate* on a still-awaiting IPO (as already true for SBIFUNDS/ALPINETEX), but is not a safe wholesale replacement for the NSE mechanism — coverage gaps, a demonstrated silent-wrong-match risk, and a largely-absent/imprecise price field. No code changed; the diagnostic script (`scripts/check_upstox_listing_date.py`, run by the operator with their own token, never touching the repo's authed calls) is left in place, untracked. |
| 2026-07-17 | Packaging build_app.py silently clobbering the real app icon | bug | **merged `2c27d8c`** | Caught by the operator from a freshly built `.exe`/`.appx` (built for the pre-.exe visual QA pass below) coming out with the wrong icon. `packaging/make_icon.py` drew the pre-V3-12 placeholder mark (plain gradient square + diamond) and was never updated when V3-12 (2026-07-16) swapped in the real "vertex-jewel" logo, whose `icon.ico`/`build/appx/*` are curated, committed binary assets with no regenerable source in the repo. `build_app.py`'s one-command build called `make_icon.py` unconditionally as step 1, overwriting the real icon with the old placeholder on every single build. Removed the call; deleted both `make_icon.py` and its appx-tile twin `make_appx_assets.py` (same bug, just not wired into `build_app.py` yet — equally wrong output if ever run) since neither can produce correct output anymore. Verified: `icon.ico` stayed byte-identical (`git status` clean) across a second rebuild after the fix. Docs (`GATE7_PACKAGING.md`) updated to match. |
| 2026-07-17 | Version bump 0.7.2 → 0.8.0 for the .exe/.appx rebuild | chore | **merged `079fbb5`** | Desktop's `package.json` was still at 0.7.2, set (`9d1eefd`) *before* the V3-16 console feature and this session's other fixes landed on `main`. Bumped `apps/desktop/package.json`, `apps/pwa/package.json`, and the hardcoded About-screen version string together (the About string is literal text in `Settings.tsx`, not derived from either `package.json`, so it would otherwise have silently kept reading "v0.7.0" regardless of the real shipped version). |
| 2026-07-17 | RHP any-host + allotment "Check allotment" landing on the actual status page | feature/bug | **merged `b359d95`, `3b46481`, `13bc784`** | Two related trust-bar changes to the external-open links, prompted by operator observation on the live app. **RHP:** every RHP now opens with the same live button `sebi.gov.in` always had — a Red Herring Prospectus is a public regulatory filing with no PAN entry, so the SEBI-only host pin was excess caution; new `isAllowedRhpUrl` (any https URL) replaces the shared check for this one case. The **registrar allowlist itself is untouched** — still strict, since that's a PAN-entry surface; the desktop `shell:openExternal` IPC gained a `kind` (`'registrar'`\|`'rhp'`) so the main-process AUTHORITATIVE gate applies the right rule per open, not one combined list. **Allotment:** "Check allotment" was opening the registrar's cached `website` field verbatim, which the upstream context cache often populates with the registrar's general homepage, not their allotment-status checker sub-page. Added a curated `REGISTRAR_ALLOTMENT_URL` map (one entry per pinned registrar), resolved by **host only** — never the cached URL's path. Two of the eight initial entries turned out wrong on operator manual-check (not guessed — verified/corrected against real reachability): `linkintime.co.in` is fully dead (the company rebranded to MUFG Intime; routed to the same live `in.mpms.mufg.com` destination as that entry), and the entire `ipo*.cameoindia.com` family was down at check time (including from Cameo's own working homepage, which still links to it) — routed to their confirmed-live homepage as a fallback rather than hand out a dead link. `kfintech.com`'s entry was also corrected (`ris.kfintech.com`→`ipostatus.kfintech.com`) on direct operator confirmation. The other 5 registrars re-verified live and correct. +test coverage for `isAllowedRhpUrl`/`registrarAllotmentUrl` (both apps). |
| 2026-07-17 | Every IPO's Detail page looked structurally different — the Reference/RHP cards appeared or vanished per IPO | bug | **merged `477d220`** (commit `e1fa599`) | Caught by the operator during the pre-.exe visual QA pass below: `Detail.tsx`'s `ContextRef` (the "Reference" card — ISIN/industry) returned `null` whenever the per-IPO context cache wasn't `available` (stale/unpublished/never-loaded), so different IPOs rendered a visibly different card layout depending on cache state alone, not on anything verdict-related. Removed the availability gate — the card now always renders once `ctx` has loaded; only the field text (via the already-existing `refField` per-field state) degrades honestly ("unknown — data stale" / "not available"), so every IPO's Detail page has the same structural card set. Verified live on all three states (present/stale/not-loaded) rendering the identical two-card layout with only the values differing. Render-layer only, no scoring-path reachability. |
| 2026-07-17 | Settings screen cleanup (Model controls removed, Console log renamed/repositioned) | polish | **merged `477d220`** (commit `f47b4d6`) | Operator-directed cleanup during the same pass: removed the redundant "Model controls" card (the app already states elsewhere that internals aren't editable); renamed "Developer" → "Console log" and dropped the repeated "Debug console log"/"Enable" label text now that the heading already says what it is; added the backtick shortcut to the "Keyboard shortcuts" card (shown only when Console log is on); swapped the Console log and Keyboard shortcuts card positions so Console log stopped CSS-grid-stretching to match the much-taller Engine card's height (it was previously paired with Engine in the same grid row). |
| 2026-07-17 | Sync-status tooltip: dropped "re-scraped" | polish | **merged `477d220`** (commit `920303b`) | Operator-requested wording tweak — the VM-fallback tooltip read "records re-scraped fresh from NSE"; dropped the word, no replacement. |
| 2026-07-17 | Pre-.exe visual QA fixtures — dev-preview-only mock engine | tooling | **built, gated, NOT merged** (branch `dev/pre-exe-visual-qa-fixtures`) | Deliberately kept off `main`. A comprehensive fixture set for walking every UI state in the browser dev preview before committing to a `.exe` build: all four verdicts (incl. the abstain case), every lifecycle stage, an allotment-stage IPO with full registrar contact fields, a verdict-history crossing, degraded/edge states (VM→local fallback, stale/unpublished/not-loaded context, an overdue-listing strand, kill-flags, a `?scenario=empty` toggle for genuinely-empty vs filter-miss), obviously-fake company names (TESTCO/MOCKON/FIXTURE LABS style) so a mock verdict can never be mistaken for a real one. Gated on `import.meta.env.DEV` (statically `false` for any `vite build`, the command Electron's `dist` packages, regardless of `--mode`) **and** a `fixtures` Vite mode — proven structurally absent from a production build (`npm run build` output grepped for every fixture marker: zero hits; no orphaned fixture chunk). This QA pass is what surfaced most of the bugs above (the icon clobber, the Detail-page card inconsistency, the RHP/allotment link issues) plus the operator's own live-app observations (the refresh spinner, console flicker/fade, Live-signals retirement). Kept as a reusable harness for re-running the "see every state" pass on future UI work, not permanent app code. |
| 2026-07-16 | V3-16 — read-only debug console ("Console log"), the LAST v3 build item | feature | **merged `1693e48`** (2026-07-16; 5 commits: `a539e77`, `684b56b`, `fecf599`, `95723e3`, `d46cf62`) | **Stage 1** (the honest-logging audit) is already merged (`ca6a6ee`); this is **Stage 2**, the screen, merged as `1693e48` on top of it. **Engine side:** an in-memory **ring buffer** (redacted at store time via the shared `redacted_payload` — the same scrub the file sink uses, so a token/PAN/auth-header can reach *neither* path; monotonic `seq` + `since` polling; bounded FIFO deque) + the existing **RotatingFileHandler** (5 MB×5 = 25 MB) now with **~14-day expiry** on top of the size cap + a GET-only **`/logs`** (default = ring live-tail with a `last_seq` cursor; `history=true` = the durable rotated files for scroll-back). **Front end:** the **Option-A terminal** overlay the operator chose — "CONSOLE LOG", column headers `ts / level / event / ipo_id / detail` aligned above their columns, INFO green · WARN amber · ERROR red, `fetch_retry ×47` **repeat-suppression** (one counted line, not a flood), **filter** by level + ipo_id/event, **one continuous timeline** (no modes): opens at the bottom **auto-tailing** the live ring (~2.5 s `since`-cursor poll, appends), and scrolling up **lazily loads older history from the disk files** — ring flowing into disk with no visible seam, stitched by timestamp with **no duplicate and no gap** (inclusive `before` cursor + client de-dupe; the recent/history toggle was dropped for this). `tail -f` auto-scroll that pauses when you scroll up and resumes at the bottom; **no "live" label** (that word was dropped, the behaviour stays); **always dark** (a dev tool; ignores the app theme); read-only. A persistent **`UiPrefs` on/off toggle** in Settings → Developer (**default OFF**, durable via the desktop config file like the awaiting-collapse fold) arms the **`` ` ``** shortcut (collision-free vs `g l`/`g u`/`t`/`?`; deadened when off). **Reads the log the engine already writes** — no second logging path. **Off the scoring path:** only `core/logging.py` changed under a protected dir, and it's the guard's allowlisted write-only sink → `MAX\|Δprob\|=0.0` (dump byte-identical vs `main`), guard clean. **Proven:** backend unit+integration tests (ring redaction/seq/eviction, file history, expiry, `/logs` over HTTP, POST→405) + frontend `node --test` (filter/collapse/detail, `devConsole` persistence + desktop restart) + `tsc` + `vite build` + a **live** run — the real engine's `/logs` served `engine_starting`→`scheduler_cycle_*`→`verdict_transition`, `since` incremental, `history` from the file, `POST /logs`→405. pytest/ruff/black/mypy/tsc/node green. **Review closed** — the two review follow-ups landed under the merge (`95723e3` live-tail auto-poll + always-dark; `d46cf62` the one continuous scroll-stream). **Shipped-`.exe` visual proof closed** — the console runs live in the shipped build (operator-confirmed 2026-07-19); three later console fixes (`2dd6bd4`, `a8895bb` under `4a03bbb`; `f47b4d6` under `477d220`) are post-merge polish on shipped code. **This closes v3.** |
| 2026-07-16 | VM-primary flip (`VM_BASE_URL`) — the shipped app depends on the VM as primary | infra | **merged `454632d`**; wiring + engine-path **live-verified**, but the built `.exe` is the operator's local build (**not yet shipped/proven in a real installer**) | The final infra step. The desktop shell bakes `VM_BASE_URL` into the engine's spawn env, so the shipped `.exe` fetches VM-first and falls back to a direct NSE scrape only when the VM is unreachable (the existing, tested per-cycle fallback). **Build-time injection keeps the IP out of the public repo:** `sidecar.ts::resolveVmBaseUrl` reads env → **gitignored** `vm-config.json` (baked into the `.exe`) → `''` (fail-safe local-only); only `vm-config.example.json` (placeholder) is committed. Live e2e against the real VM: up → both stores `source=vm`; forced-unreachable → `source=local`, bounded, no hang. **Reversible:** `VM_BASE_URL=` empty (no rebuild), or blank the config + rebuild. |
| 2026-07-16 | Keepalive tuned 120s→240s (Oracle idle-reclaim margin) | infra (tuning) | applied on the box + **burn-duration verified** (measured 240s); docs merged `0be8c5f`; **multi-day console P95 confirmation PENDING (operator)** | Reclaim reality corrected vs the blueprint: the shape is **E2.1.Micro = 1 OCPU = 2 vCPU** (a single-thread burn reads ~51% instance CPU, not 100%), and Oracle's rule is **CPU p95 < 20% over a 7-day window** (not "~15%"). 120s was marginal (~20.5% under 5-min aggregation); 240s/30-min = 13.3% duty, ~41%/bucket — comfortable margin at ~7% avg CPU on one `nice`-19 thread. The timer is **box-only config**; only the docstring/runbook sync was a repo change. Verify: console `CpuUtilization` P95, 1-min interval, 7-day ≥ 20%. |
| 2026-07-16 | Digest schedule 2×→4×/day | infra (tuning) | applied (box-only timer) + docs merged `43f2077`; **live** | `ipo-telegram-digest.timer` 00:00 & 18:00 → **00:00 / 06:00 / 12:00 / 18:00 IST**. A clean one-line `OnCalendar` add; nothing assumed the 12-hour gap — the state-change alert-check runs independently every 20 min. |
| 2026-07-16 | New-IPO onset context trigger | feature | **merged `f658948` + deployed**; **live-proof PENDING** — never observed firing on a genuine new IPO (none has appeared since deploy) | Closes the mid-interval onset gap where a new IPO shows empty context for hours. The 30-min NSE ingest diffs the records store's `ipo_id`s pre/post-upsert; a genuinely-new id fires **one** immediate single-symbol context pull, **merge-written** so other IPOs' context and the global `refreshed_at` stay untouched (the one real correctness risk — tested). "New" is derived from `ipo_records.parquet` itself (restart-safe, no separate seen-set file). Dark-ships without `UPSTOX_TOKEN` (present on the box). A firing is captured in journald (~90-day retention), greppable (`new_ipo_context_pull*`). |
| 2026-07-16 | **V3-2 REVISED — VM-write archive snapshot (replaces the desktop-drop)** | infra | **merged `5fc1870` + live-verified** (a push landed both files in `ipo-archive`; idempotent no-op on rerun) | **Supersedes the 2026-07-15 V3-2 desktop-drop→VM-pull design (retired).** LINEAGE (preserved deliberately, not smoothed): desktop-drop built + merged (`0d24237`) → VM-owned Option A → **dropped** (durability-vs-plumbing tradeoff — a two-source cross-check wasn't worth the plumbing for a private tool) → **revived as a VM-write snapshot**: the VM itself pushes `ipo_records.parquet` + `ipo_context.json` daily to `ipo-archive`. The archive deploy key flipped **read-only → write**; the original "a compromised VM can't corrupt the archive" guarantee is **replaced by append-only branch protection** (force-push + deletion blocked) on the now-**public** repo (branch protection is Pro-only for private repos on this account). Single-writer → a durable off-box copy, **not** the two-source cross-check the original design implied — a knowing tradeoff (durability-against-reclaim, not tamper-proofing). Related drop: a **scorecard / faithful point-in-time history** build was considered and **dropped** in favor of backtest-from-what's-re-fetchable (same not-worth-the-plumbing reasoning). |
| 2026-07-16 | VM host/IP scrubbed from the public repo | security/ops | **merged `cb840f5`** + history rewritten (`git filter-repo`) | `ipo-advisor` is public; the real VM IP + SSH user were scrubbed from **all** history (filter-repo — verified zero IP in any commit) and replaced with `<VM_IP>` / `<vm-user>` placeholders. Real values live in a local/private operator note. This underpins the VM-primary flip's build-time injection — the IP never enters public source. |
| 2026-07-15 | **V3-3 alerting realized as a Telegram subsystem (5 slices)** | feature | **merged `d44ac7c` + live on the VM** (digest fires + delivers) | The blueprint's one-line "scrape-failure alerting (email/Telegram/etc)" was built as a full interactive subsystem: a **health digest** (periodic reconciler, now 4×/day), **immediate state-change alerts** (one per edge, no cry-wolf; 20-min alert-check), and **owner-only commands** `/status` (VM health snapshot) + `/login` (record the ≤30-day Oracle console login). Single-source `/` command menu (`setMyCommands`). **3 systemd units** — `ipo-telegram-bot` (long-poll daemon, `Restart=always`), `ipo-telegram-digest.timer`, `ipo-alert-check.timer` — with token/chat-id in `/etc/ipo/telegram.env`; **dark-ships** without creds. Deploy runbook: `operations/README.md`. |
| 2026-07-15 | V3-2/V3-3 two-repo, two-key split | infra (security) | **merged `be53dde`** | The archive key was **read-only** (a compromised VM couldn't corrupt the durable archive — the original V3-2 posture); the heartbeat needs a **write** key, so it got its **own throwaway repo** (blast radius = liveness pings only). A read-only archive key can't coexist with a shared heartbeat-write key. **NB:** the archive key later flipped to **write** for the snapshot revival (2026-07-16, see above) — the read-only posture was traded for append-only branch protection. |
| 2026-07-15 | V3-4 — weekly fallback self-test (proves the local-scrape path hasn't rotted) | feature | **merged `6f393f0`**; **ships dark** — closes the V3-1→V3-4 CODE track (deploy-side: a weekly desktop task) | V3-1 owns the fallback client; this is the periodic **proof it still works**, so the local-scrape code — which runs ONLY when the VM is down — doesn't rot undiscovered until the day you need it. **Drives the REAL path, only the trigger faked:** [fallback_selftest.py](../../scripts/fallback_selftest.py) calls the genuine `refresh_data_plane` with a `VmClient` that raises `VmUnavailable` (exactly as a down VM does), so it takes its `except VmUnavailable` branch → `refresh_from_nse` → `build_live_records` → the real `NseClient` → real NSE → validation → write — no mock of the fallback, no manufactured outage (the VM can be up and serving; the fallback is *selectable*). **Asserts valid data, not "it ran":** `last_attempt_ok=True` + `source=local` + well-formed records with a count; a feed-shape change or broken dependency flips it to FAILED. **Isolated + self-cleaning:** a per-run `TemporaryDirectory` scratch store (never the live `data_store/`) torn down in a `finally` — a weekly run never leaks a scratch dir. **Same polite discipline** as the live ingest (the identical `config.scrape` `NseClient` — one hit a week, no separate unthrottled path) with an isolated throwaway raw cache. **Fails loud in the ritual you run:** the verdict is recorded to `fallback_selftest.json`; `run_heartbeat --fallback-selftest` names it honestly — FAILED → `[STALE] local fallback - self-test FAILED …` (exit 1); the weekly-test-stopped case → `… stale … weekly test not running?` (exit 1, the meta-failure caught); healthy → `[OK] … passed (N records)`. **Ships dark:** purely additive; `run_heartbeat` checks it only with `--fallback-selftest`, no scheduled task → nothing runs; the engine imports nothing new at scoring time (byte-identical; MAX \|Δprob\|=0.0, machine-checked; scoring-path guard clean); `ipo.service.fallback_selftest` sits in the already-fenced service layer. **CI proves both directions** (the important half being the rot signal): canned-valid NSE → `ok=True`; canned-`SourceError` → `ok=False`. +9 tests incl. passes-on-working, fails-on-rotted, cleans-up-even-on-failure, surfacing (failed/stale/never-run/fresh), persistence — plus a **live** demo (working → `[OK] passed (1 records)`, exit 0; rotted → `[STALE] FAILED`, exit 1). Shared the `ago` humanizer into `ipo.service.heartbeat` (one definition, V3-3 + V3-4). pytest/ruff/mypy/black green. **Deploy-side (operator):** a WEEKLY desktop scheduled task + `--fallback-selftest` in the ritual. |
| 2026-07-15 | V3-3 — VM monitoring: heartbeat-staleness detector + out-of-band liveness ping + Oracle keepalive | feature | **merged `18df49e`**; **ships dark**. Alerting was realized as the **Telegram subsystem** (2026-07-15, `d44ac7c`); keepalive later **tuned 120→240s** (2026-07-16, `0be8c5f`) — see rows above | Keeps the VM data plane visible and alive, via two independent, dark-shipping mechanisms. **Primary — heartbeat-staleness detector (always on, can't-itself-fail):** the VM writes a small `heartbeat.json` (beat time, ingest freshness from the BUG-1 `ingest_state`, free disk, keepalive marker) each cycle and pushes it to a git channel; the desktop's `run_heartbeat --vm-heartbeat` reads it and **names every failure honestly** — `VM heartbeat - last beat 1d ago — fetch may have stopped`, `VM ingest - NSE fetch failing …`, `VM disk - 4% free …`, `VM keepalive - … Oracle reclaim risk` — never a generic "unhealthy", and **exits non-zero**. Needs *nothing alive on the VM* at check time and *no VM credentials on the desktop* (a git-history read), so it can't fail the way a monitor-on-the-box would; it carries essentially every failure mode. **Backup — one out-of-band liveness ping:** for the single unrecoverable, time-sensitive case (Oracle reclaiming the instance — a reclaimed VM doesn't come back), [vm_heartbeat.py](../../scripts/vm_heartbeat.py) pings a free dead-man's-switch (`HC_PING_URL`, healthchecks.io) each cycle; the monitor emails when pings *stop*. One liveness bit, not an alerting stack. **Keepalive (load-bearing):** Oracle reclaims Always-Free compute that AVERAGES idle over a window (a brief spike doesn't help), so [vm_keepalive.py](../../scripts/vm_keepalive.py) runs a bounded, hard-capped (≤600s) CPU burn and touches a marker the heartbeat checks — a *dead keepalive is itself a fail-loud signal* before reclamation. **Ships dark & downstream:** purely additive — the engine imports nothing new at scoring time (byte-identical; MAX \|Δprob\|=0.0, machine-checked; scoring-path guard clean); the detector is inert without `--vm-heartbeat`, the ping without `HC_PING_URL`, the writer/keepalive without their systemd timers. `ipo.service.vm_health` sits in the already-fenced service layer (the transitive import guard keeps it off the scoring path). **Built + tested now:** `ipo.service.vm_health` (model + honest-naming detector + read + ping), the two scripts, and the `run_heartbeat --vm-heartbeat` integration; 13 tests incl. the headline **fires-on-stale / quiet-on-fresh** proof, honest-naming per dimension, corrupt→stale, dark-ship ping, writer-reflects-`ingest_state`, keepalive-bounded — plus a **live** demo (fresh → all VM lines OK, exit 0; stale → `VM heartbeat STALE` + `ATTENTION DEGRADED`, exit 1). pytest/ruff/mypy/black green. **Deploy-side (operator, per runbook):** the heartbeat git channel (a `heartbeat` branch of the archive repo), the healthchecks ping URL, the VM systemd timers (heartbeat + keepalive), keepalive tuning verified against the Oracle console CPU metric, and a **HARD ≤30-day Oracle-console-login calendar item** (no script can prevent account-level reclaim). |
| 2026-07-15 | V3-2 — durable archive (app history → private git rendezvous → VM append-merge) | feature | merged `0d24237`, then **SUPERSEDED 2026-07-16 by the VM-write snapshot (`5fc1870`)** — this desktop-drop→VM-pull design is **retired**; the lineage + reasoning are preserved in the revised row above | *(Historical — the design as originally built, now replaced. Kept for the decision trail; see the 2026-07-16 "V3-2 REVISED" row for why the desktop-drop is dead and the VM writes the snapshot instead.)* The **output half** of the data plane (V3-1 was the input half): the app's one non-reproducible artifact — `verdict_transitions.json`, *when* each verdict changed — is backed up so it outlives the local machine, **without** giving the app any write access to the durable copy. Shape: a daily Windows scheduled task ([archive_drop.ps1](../../scripts/archive_drop.ps1)) **drops** the file outbound (git push) to a **private git rendezvous** (a separate repo — operational data, deliberately NOT a branch of the code repo); a daily VM systemd timer **pulls → validates → append-merges** it into a durable archive the app can never touch ([archive_pull.py](../../scripts/archive_pull.py) over [ipo.archive](../../src/ipo/archive)). **Append-only, not mirror:** the merge is a union keyed on the whole transition, so it is idempotent + order-independent — a missed / doubled / out-of-order pull all converge, and a partial or lost local copy can only add, never delete (the same never-prune-the-log discipline as BUG 2). **Don't trust the drop:** a malformed/truncated rendezvous file is REJECTED whole (exit 1, nothing merged) — the archive's integrity never depends on the drop being well-formed (V3-1's "don't trust a 200", on the pull side); a leading UTF-8 BOM is tolerated at the read boundary (`utf-8-sig`). **Ships dark:** purely additive — the engine imports nothing from `ipo.archive` (byte-identical; MAX \|Δprob\|=0.0 by construction, machine-checked: 0 changes under the scoring path), and the dropper is a silent no-op with no rendezvous configured — inert until deployed. **Strictly downstream of verdicts:** written *from* transitions, never read into the feature vector — proven by the import-boundary guard (extended so `ipo.archive` is unreachable from features/model/calibration/core, alongside the context store) + a direct-reference scan. **Read-API untouched** (still GET-only); **credentials are deploy-side** (the SSH deploy key lives only in the scheduled task's + the VM's environment, in the operations runbook, never committed). +tests: validation (rejects non-JSON / truncated / bad-row / non-array; tolerates BOM), merge (dedupe, idempotent double-pull, order-independent out-of-order pull, append-only-never-deletes), store round-trip + corrupt-raises, pull-merge end-to-end + rejection-leaves-archive-untouched, boundary guard; the PowerShell dropper smoke-verified locally (drop→push, idempotent, dark-ship no-op — not in CI, which is ubuntu). pytest/ruff/mypy/black green; full loop verified end-to-end. **Deploy-side (operator, per runbook):** create the private repo, set the deploy key, schedule the local drop + the VM pull. |
| 2026-07-15 | V3-1 — VM data plane (app side): read-API + client + honest fallback + source indicator | feature | **shipped in 3 steps** (tags `v3-1-vm-read-api` / `v3-1-vm-client` / `v3-1-fallback-chip`); shipped dark, then **flipped to VM-primary-by-default in the build** (2026-07-16, `454632d`) — the app now depends on the VM as primary; see the flip row above. VM side later completed (records timer `run_live_ingest.py` `cb04947`; context timer live `a1a4675`) | The VM data plane's **app side**, merged in three steps. **Step 1** (`v3-1-vm-read-api`): a GET-only FastAPI read-API ([vm/server.py](../../src/ipo/vm/server.py)) — `/health`, `/records`, `/context`, each a `{refreshed_at, data}` envelope so freshness travels *with* the data; no mutation route and no engine import, so the VM **structurally cannot run the model or be made to act** (BUG 1's door, re-refused). **Step 2** (`v3-1-vm-client`): `VmClient` (10s timeout, 2 retries) + a per-cycle **VM-primary / honest-local-fallback** refresh ([data/ingest/data_plane.py](../../src/ipo/data/ingest/data_plane.py)) — records fall back to a fresh NSE re-scrape (`source=local`), context keeps the last-known cache and **ages honestly** (`context_source=local`) because the Upstox token lives on the VM; each store's provenance is recorded on `/status` (extends BUG 1's `IngestState`). *Don't trust a 200* — the envelope is schema-validated and a malformed/truncated body triggers fallback (tested). **Step 3** (`v3-1-fallback-chip`): the source state folded into the **ONE** SyncStatus chip (never a second, contradicting chip) — quiet when healthy; on a store fallback it appends the honest per-store split (records fresh vs context aging) and turns amber, flagging **only what the `Updated …` timestamp can't already convey**. **Ships dark:** with no `VM_BASE_URL` (`vm_client=None`) the engine behaves exactly as before the VM existed — pure local scrape, `context_source` cleared to `null`, no chip — so this merges **inert** until the VM is deployed. **Enable** by setting `VM_BASE_URL` (read directly by the runner — deliberately NOT an `IPO_*` key, so VM wiring can't leak into `AppConfig`). All three states **live-verified against a real `/status`**: both-VM (both stores pulled over real HTTP from `ipo.vm.server`), dark-ship (no chip), VM-down (amber split). Step 3 **frontend-only** — MAX \|Δprob\|=0.0 by construction (machine-checked: 0 Python in the diff) + scoring-path guard OK; steps 1–2 scoring-path untouched (git-proven + guard). The **output half** (app history → durable VM archive) is deliberately NOT here — that's **V3-2**, and by design the **VM pulls** (the app never pushes; the read-API stays GET-only). +tests: VM server + envelope validation + malformed-fallback + dark-ship no-op (pytest), `fallbackStatus` 3-state (`node --test`, 18/18); pytest/ruff/mypy/tsc green. |
| 2026-07-14 | V3-10 — anchor investor list (context only) | feature | **DROPPED — no probes; even perfect data changes nothing** | **No Upstox source, and no reason to seek another.** Upstox's IPO API has exactly two documented endpoints (*Get IPOs*, *Get IPO Details*); the details response is the same 26 fields already mapped (registrar / rhp_url / lot_size / isin / industry / cut_off_price / status / timeline / …) with **no anchor field** — there is no anchor route to probe. **Do not re-check.** Chittorgarh *does* carry anchor lists, but V3-10 was dropped **without diagnosing them** because even perfect data would not change the call: **(1) Redundant with QIB.** `anchor_quality` was tested as a model feature and deferred (data-limited) with a strong QIB-redundancy prior — anchors who commit pre-open also bid, so their conviction is already inside the QIB subscription number; it is not new information, just *earlier* information. **(2) Superseded at the decision clock.** The anchor list is a *pre-open* sentiment signal, but our verdict is delivered at **close of bidding**, when the full book is in — by which point the anchor list is stale and irrelevant (a guess at the very thing we then measure directly). **(3) The only feature that would need a NEW permanent dependency.** Every other v3 feature came free off NSE or Upstox; V3-10 alone would require a permanent Chittorgarh scraper the VM must run and keep alive forever (breaking on every layout change) — for a display-only nicety. **Do not re-propose.** |
| 2026-07-14 | Finding-④ — listing-resolution **strand detector** (Option A; BUG-1-class silent failure) | bug (correctness) | **merged `7d06c4c`** (commit `9f680fd`) | Surfaced by the `status` cross-check thread and diagnosed first. The Live→History resolution (`resolve_listings`) can fail with **no signal**: **Mode 1** — the book closes but NSE past-issues never yields a matching symbol, so `listing_date` is never stamped; **Mode 2** — `listing_date` is stamped but the bhavcopy price never backfills past `PRICE_BACKFILL_DAYS`. Either way the IPO sits forever under an honest-looking "awaiting listing" label. **The path itself works** — traced end-to-end + a new integration test (awaiting → `resolve_listings` → leaves the awaiting surface → in History with the correct net return + HIT/MISS), and the dev boot pull was observed resolving a stale `knack` fixture live — and **the real store is currently clean** (0 strands; Kusumgar/Laser Power are legitimately inside T+3, not stranded). The defect was that **failure is silent** (the BUG-1 lesson: a path that can fail while looking fine). Fix (**Option A**, self-contained — our own dates only, no second source): one predicate `listing_overdue_state` ([service/lifecycle.py](../../src/ipo/service/lifecycle.py)) covering **both** modes, with a **holiday-aware trading-day grace buffer** (T+3 + 2 trading days via the NSE calendar) so a legit one-day slip doesn't cry wolf — the same "flag a real departure, not noise" discipline as the drift monitor's Wilson trigger. Surfaced in **two** places: a `listing_overdue` board flag → History's awaiting section names the strand honestly ("listing overdue — resolution may have failed" / "listing outcome overdue — price never recorded") with an overdue **count + red badge**; and `run_heartbeat.py --data-dir` prints an `[OVERDUE]` block and **exits non-zero**, so a strand appears in the operator ritual, not weeks later. **Option B (Upstox `status` drift-flag) considered, NOT built** — its only marginal gain over A is distinguishing "we missed it" from "listing genuinely delayed": real but low-value for a single-operator app, and it adds a dependency on a manually-refreshed cache, whereas A works even if Upstox/the cache is down. **Do not re-propose B.** Scoring path untouched (git-proven; MAX \|Δprob\|=0.0 across 386). +tests: lifecycle classifier (both modes, buffer, holiday-aware, real parquet store), board overdue flag, `awaitingLabel` via `node --test`; heartbeat smoke-verified (loud, exit 1); UI verified live (badge + honest label, no console errors). |
| 2026-07-14 | V3-11 — minor context fields (`isin`, `industry`) + `status` / `cut_off_price` decisions | feature | **merged `ba9bba4`** (commit `5d174d6`, tag `v3-11-context-fields`); `status`(B) & `cut_off_price` **considered-not-built** | `isin` + `industry` shipped as plain display metadata through the SAME per-IPO context store (one shared `field_state` rule, **no source named**, honest degradation) — a compact "Reference" card on the detail page, verified live. **`cut_off_price` NOT built** — our `issue_price` **is** `price_band_high` ([core/types.py](../../src/ipo/core/types.py)) and Upstox `cut_off_price` == band high in every sample; the detail page already renders the band, so it would re-display a value we already hold → doesn't earn its place. **`status` NOT built as a display field** — the diagnosis showed it is too coarse to arbitrate our lifecycle (the whole 394-IPO snapshot is `{closed, listed}`) and our derived status is pure date arithmetic (correct when the dates are right); the real gap it *hinted* at — silent resolution strands — is fixed directly by **finding-④** instead of by importing an external status. Also: pinned `mypy>=1.9,<2` — the unbounded pin let mypy 2.x (2.1.0) silently redden the CI gate on `main` (stricter bare-generic checks) with no code change; fixed the two affected files' annotations and capped the version (a dependency that can break your own gate is itself the silent-failure class). |
| 2026-07-14 | V3-8 — `lot_size` (Upstox **sole source** — NOT a fallback) | feature | merged to `main`, tag `v3-8-lot-size` | **Reframed by the diagnosis** (renamed from "lot_size fallback"): NSE provides `lot_size` on **0% of IPOs** — its `ipo-current-issue`/`all-upcoming-issues` endpoints carry no lot field, and the backfill CSV column is 0/386. So there is **no NSE value to be authoritative anywhere** — the blueprint's "NSE remains authoritative where it has a value" rule is a **dead requirement** (drop it) and Upstox is the sole source for 100% of IPOs. **The agreement check (NSE-vs-Upstox) was impossible** (zero NSE values), so it was correctly substituted with a **regulatory-invariant** corroboration: SEBI's ~₹14–15k minimum retail application means a correct `lot × cut-off` must land there — **22/22 sampled Upstox lots do** (₹13,420–15,000, tightly clustered at ₹14.6–15k, exactly what issuer-chosen lots look like). **Sufficient** because `lot_size` is **static** (fixed at RHP filing — none of the stale-snapshot failure mode that sank `total_subscription` in Check-1) and Upstox reads it from the same RHP field the exchange uses; an exact second-source (Chittorgarh) check was **initially judged not warranted** (the app places no bids, so the broker enforces the true lot at application time — an off-by-one is a ~₹200 planning delta, not an invalid bid). **Reversed by operator request and run post-hoc (2026-07-14): 20/20 EXACT agreement** — Chittorgarh's exact integer lot equals the shipped Upstox lot for every one of the 20 mainboard IPOs (REIT `BAGMANE` + InvIT `RIIT` excluded: trust *units*, not a like-for-like share lot), each application amount inside the SEBI ₹13,420–15,000 band. The regulatory-invariant corroboration is thereby **upgraded from "the application amount is right" to an exact confirmation of the integer itself** — zero disagreements, nothing to correct. Evidence: `research/chittorgarh_lot_crosscheck.json` (URLs discovered from Chittorgarh's rendered 2026 mainboard list; lot read from each detail page's "Retail (Min)" row). Flows through the **same context store** (`IpoContext.lot_size`, `refresh_context.py` captures it, one `field_state` rule, `GET /context/{id}`). **UI (operator-set):** no source attribution (no "via Upstox"); shown as an **indicative** figure — "Lot (indicative): ≈ 70 shares · approx ₹14,980" — so a possibly-imprecise value never wears an authoritative face. Scoring path untouched (MAX \|Δprob\|=0.0); pytest 308, ruff/mypy/tsc green; verified live (indicative lot renders, no provider named, no console errors). |
| 2026-07-14 | V3-5 — RHP/DRHP direct links | feature | merged to `main`, tag `v3-5-rhp-links` | Data-shape diagnosed first (operator-run probe): valid Upstox pre-close statuses are `upcoming`/`open`; `rhp_url` is fully populated from `open` onward (open/closed/listed 100%, real SEBI/issuer URLs), absent on `upcoming`. **DRHP dropped (RHP-only):** `drhp_url` = 1/28, and the lone case is an `upcoming` IPO with symbol `'NONE'` — **unjoinable even where it exists**, so it's a non-field, not sparse data. **Generalized the V3-6 registrar cache into ONE per-IPO Upstox context cache** (Option A) — `registrar_info.json`→`ipo_context.json`, `AllotmentStore`→`ContextStore`, `refresh_allotment.py`→`refresh_context.py` (queries open+closed+listed, excludes `'NONE'`-symbol at ingest), one `field_state` staleness rule shared by registrar + RHP + future fields (V3-8/11/10 add a field, not a path). New `GET /context/{id}` serves the RHP + its state; Detail page shows it labelled **"Red Herring Prospectus (RHP)"** (never generic, never the draft). **RHP-open security:** the openExternal allowlist gains the SEBI filing host (`sebi.gov.in`) → one-click for SEBI-filed RHPs; issuer-hosted RHPs render inert copyable text (unbounded domains, can't vouch). Requirements met: transitive guard generalized to "no `ipo.service.*` reachable from the scoring path" (rename-proof); one staleness rule; VM-runnable refresh; symbol-keyed join with `'NONE'` excluded at ingest; allotment surface unchanged (verified live + MAX \|Δprob\|=0.0). +tests (allowlist SEBI/issuer, `build_ipo_context` RHP states, `/context` endpoint); pytest/ruff/mypy/tsc/node --test green; verified live (RHP button → SEBI, allotment deep-link unchanged, no console errors). |
| 2026-07-14 | V3-6 — Allotment tab | feature | merged to `main`, tag `v3-6-allotment-tab` | Data-shape diagnosed first (operator-run probe): `registrar_info` is real, 100% populated across the sample, trustworthy values (MUFG Intime / Bigshare / KFin with their real portals) — GREEN, unlike Check-1's stale `total_subscription`. New top-level tab (Live → Upcoming → **Allotment** → History) listing IPOs at/past the allotment stage (closed + awaiting + listed ≤7 days, via `status.ts` scope) with registrar name, a **deep-link to the registrar's own allotment-check page**, and a collapsible grievance contact. **Zero PAN** — the app links out (new `shell:openExternal` IPC, https-validated); the user enters PAN on the registrar's site. **Option B data path (VM-discipline):** the app only READS a token-free cache in the data plane (`<data_dir>/allotment/registrar_info.json`); the fetch is an external, VM-runnable script (`scripts/refresh_allotment.py` — pure/portable, env token, `--data-dir`), so when the VM lands it becomes VM-primary/local-fallback with every other feed (one pattern, trivial swap). **Three structural guarantees:** (1) registrar data physically can't reach the model — own store/type, never an `IPORecord` field; proven by `test_scoring_path_cannot_reach_registrar_data` (permanent import-graph guard); (2) degrades honestly (no cache → `available=false`; per-IPO no entry → `registrar=null`; freshness line); (3) severable (read-only, off the scheduler — Upstox down never touches verdicts). Scoring path untouched three ways: git-proven, import-graph proven, **MAX \|Δprob\|=0.0 across 386 IPOs**. +15 tests (297→312); pytest/ruff/mypy/tsc green; verified live (tab renders, join works, deep-link external not in-app, no PAN input, no console errors). *Carry-forward for V3-5:* re-run the probe on `upcoming`/`open` to check `drhp_url` there (0/22 on closed/listed is expected — superseded by RHP) before finalizing "show whichever document exists." |
| 2026-07-14 | V3-7 — `mandate_end_date` → replace the approximated close-day cutoff | feature (correctness check) | **NOT ADOPTED — sourced value changes nothing** | Diagnosis-first, and the blueprint's premise was refuted (3rd time this run). The premise: an "approximated SEBI cutoff" controls *when* the book-closed scoring cycle fires, and a sourced `mandate_end_date` would improve it. Reality: **`book_closed = asof_date >= close_date`** is **date-level** ([features/build.py](../../src/ipo/features/build.py)) — it flips at midnight of the close day, not at any cutoff time. The `_CLOSE_EOD_HOUR = 18` constant ([service/engine.py](../../src/ipo/service/engine.py) `decision_asof`) is **only a date-anchoring device** — it freezes the as-of clock at `close_date @ 18:00` so the decision snapshot stays pinned to the close day. **`asof.time()` is never read anywhere** in `features/`, `engine.py`, or `model/` (grep-verified); every feature is date-level (book_closed, GMP [None live], regime, subscription off the record). So the cutoff *hour* has **zero** effect on any feature/probability/verdict, and replacing it with a sourced `mandate_end_date` (17:00/17:30/whatever) would change nothing. Field not verified — and deliberately not: Check-1's Upstox pull never captured `mandate_end_date` (only `total_subscription`/`status`), and verifying a field that cannot matter is wasted effort (esp. given Check-1's endpoint-wide staleness). **Do not re-chase:** `_CLOSE_EOD_HOUR` is date-anchoring only; its exact minute is immaterial by construction. No code touched; research branch deleted. **Reconfirmed 2026-07-14 by the V3-6 probe** — `mandate_end_date` is not merely "changes nothing," it is **semantically not a close-day cutoff at all**: for an IPO listing 2026-07-16 the field reads `2026-08-24` (the ASBA bank-mandate *validity* end, ~a month after listing). The real bidding close is `timeline.application_end_date`. Had we adopted `mandate_end_date` as the cutoff we'd have wired in a nonsense date. Do not wire it as a close cutoff. |
| 2026-07-14 | BUG 3 — theme toggle breaks after changing theme in Settings | bug | fixed (merged to `main`, tag `bug-3-theme-toggle`) | Diagnosis-first — and the empirical check **refuted** the initial hypothesis (as BUG 1's did). Suspected two-writers-without-a-shared-store; in fact both writers already route through the shared `setThemeMode`→`prefs.ts`. Real defect: stale **read-side** caches — the header `ThemeToggle` (`useState(resolved)`) and Settings (`useState(theme)`) each held a private copy updated only by their own clicks, so after an external change (Settings, or the `t` shortcut) the toggle acted on a stale value → the **first click was swallowed** (off-by-one). Reproduced live before fixing. Fix: made `prefs.ts` a small subscribable store (`subscribe`/`notify` on every persist) + a `useThemeMode()` = `useSyncExternalStore(subscribe, getThemeMode)`; deleted both private caches so the toggle, Settings, and the (already-correct) `t` shortcut agree by construction. Ruled out (didn't fit evidence to theory): hydration ordering (main.tsx awaits hydrate→applyTheme→mount), CSS-class leak (setAttribute replace), system-race (see known limitation below). Scope held to theme only — a scan found the other five prefs caches (density/notifications/costs/startup/pinned) share the shape but are **single-writer**, so latent-only; a caveat comment at the `prefs.ts` getters warns to convert them if a 2nd writer is ever added. GATE BUG-3: `prefs.test.ts` (store core, DOM-shimmed) via `node --test` — 4/4. PWA-only: scoring path untouched by construction; tsc + node --test (10/10) green; verified live — Settings→toggle **and** toggle→Settings **and** `t` shortcut all consistent, no swallowed click, no console errors. |
| 2026-07-14 | BUG 2 — alerts accumulate without limit | bug | fixed (merged to `main`, tag `bug-2-alert-retention`) | Diagnosis-first. Root cause: the alert center's "Recent APPLY crossings" rendered the entire append-only transition log with no relevance scoping or per-IPO dedupe; the client `alertsSeen`/`notifiedCrossings` sets also grew unbounded. **Not** the BUG 3 two-writers shape — the alert data is single-sourced (the transition log) and each client set has one writer tracking a different thing; this is an absence-of-retention failure. The log is load-bearing (detail-view per-IPO history + scheduler priming), so the fix scopes the **view**, never prunes the store. Retention rule (operator-adjusted): keep while the outcome is UNRESOLVED — Open/Closes-Today/**Awaiting-listing** all kept, only **Listed** drops (coherent with the V3-6 Allotment tab; Awaiting is a bounded ~T+3 window). Extracted `statusLabel` + new `alertRelevant`/`isListed` into shared `status.ts` (killed a third drifting copy in History.tsx); pure selection logic in `alerts.ts` used by both the alert center and native notifications (tested logic == shipped). GATE BUG-2: `alerts.test.ts` (multi-IPO cycle) via `node --test` — 6/6. PWA-only: scoring path untouched by construction (no engine/Python/model/config files); pytest 286 green, tsc + node --test green; verified live (alert center renders, awaiting-listing retained, no console errors). |
| 2026-07-14 | BUG 1 — stale verdicts under a fresh-looking timestamp | bug | fixed (merged to `main`, tag `bug-1-freshness-truth`) | Diagnosis-first. Defect 2 (freshness truth): the chip was bound to react-query `dataUpdatedAt` (local API-answer time) while `refresh_from_nse` never surfaced failure — structurally unable to report staleness. Added `IngestStateStore` + `GET /status`; `last_success` advances only on a confirmed NSE pull, persisted, failures visible ("… · retrying"). Defect 1 (on-open refresh): nothing client-side could trigger a real pull (empirically confirmed — 29 `/board` reads → 0 NSE fetches; only the scheduler timer/boot pull). Shell-owned trigger over a parent-only stdin channel on window focus/restore + Refresh button; engine-side debounce (~15 s). Renderer stays read-only, API stays GET-only. Scoring path byte-identical: MAX \|Δprob\|=0.0 across 386 IPOs, git-proven (nothing under model/calibration/features/core, models/, config/). +12 tests; ruff/mypy/tsc green; verified live (green chip) + stdin-channel harness (debounce + real pull). |

---

## Known minor limitations (documented, not bugs to chase)

- **Theme `system` mode does not track runtime OS-theme changes.** With theme set to *System*, the app resolves light/dark once (via `matchMedia('(prefers-color-scheme: dark)')`) at load / on each explicit theme change, but there is **no live `matchMedia` change-listener**, so flipping the OS between light and dark while the app is open does not re-theme it until the next reload or manual theme change. Surfaced during the BUG 3 diagnosis (2026-07-14) and deliberately **not** bundled into that fix — it is a separate, latent behavior, not the toggle desync. If it ever becomes annoying it is a one-line fix: add a `matchMedia(...).addEventListener('change', …)` that re-applies the theme while mode is `system`. Recorded here so it is a known behavior rather than a future mystery bug.
